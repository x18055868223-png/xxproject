# 备份推送记录：2026-06-19 r2.1

备份版本：`NRD-XXPROJECT-BACKUP-2026.06.19-r2.1`

目标仓库：`https://github.com/x18055868223-png/xxproject`

## 本轮目的

r2.1 是一次全量文档核验与版本锚定更新。它不改变代码运行行为，目标是让仓库文档和当前真实运行链路对齐，避免旧版本号、旧部署路径和历史设计稿误导后续部署或排障。

## 当前锚点

| 模块 | r2.1 当前口径 |
| --- | --- |
| FMZ 信号层 | `demo_version=1.3.0`；`schema_version=nrd.schema.v1.0.0`；只读观察；FMZ 内不调用 LLM |
| GEX API | `gexmonitorapi=0.2.0`；`/v1/info` 含 rank；rank 窗口 `rolling_30d_or_available` |
| 审计前端 | `signal_cards/index.json` + 单卡 JSON + `fallback.js` |
| Materializer | 默认读取 `/home/bitnami/fmz2/logs/storage/668422/demo/logs/signal_review.jsonl` |
| LLM sidecar | Gemini `gemini-3.5-flash`；`signal_llm_review@1.2.0`；`gemini_signal_review_prompt@1.2.0` |
| 执行层 | `STRATEGY_VERSION=2.5.0`；交易门默认关闭；未正式启用 |
| 自检脚本 | `tools/server_self_check_signal_stack.sh`；默认只读，`--run-oneshots` 才触发 sidecar/materializer |

## 本轮文档处理

1. 新增 `00_总纲/中性回路工程总纲_v2026.06.19-r2.1.md` 作为当前主入口。
2. 更新 `README.md`、`README_XXPROJECT_BACKUP.md`、`BACKUP_VERSION.json`、`99_工程资产索引/README.md` 的 r2.1 标记。
3. 修正 `PROJECT_MEMORY.md` 中执行层当前决策仍停在 v2.0.0 的过期口径。
4. 给历史总纲和早期模块总览增加“历史参考，不作当前运行口径”的阅读边界。
5. 保留历史文档原版本号作为追溯事实，不把历史设计稿强行改写成当前运行事实。

## 服务器运行基线

```text
/home/bitnami/fmz2/logs/storage/668422/demo/logs/signal_review.jsonl
/opt/signal-audit
/opt/signal-audit-tools
/opt/signal-audit-tools/signal_llm_reviews.jsonl
/etc/gexmonitorapi.env
/etc/signal-audit/llm.env
```

## 安全边界

- 不提交真实 `GEMINI_API_KEY`。
- 不提交真实 GEX `API_TOKEN`。
- 不提交生产 `signal_review.jsonl` 或 `signal_llm_reviews.jsonl`。
- 不提交 `.env`、缓存、虚拟环境或服务器运行覆盖配置。

## 验收口径

- JSON 版本文件可解析。
- 文档中当前入口均指向 r2.1。
- 历史文档有明确阅读边界。
- 敏感信息扫描无真实 key/token 命中。
- 关键 Python 文件仍能编译。
- r2.1 变更推送到 `xxproject/main`。
