# 备份推送记录 2026-06-19

备份版本：`NRD-XXPROJECT-BACKUP-2026.06.19-r2`

目标仓库：`https://github.com/x18055868223-png/xxproject`

## 本轮整理动作

1. 将 `05_GEX监控API_数据增强接口/` 更新为本地 GEX Monitor API 工作区的 rank-enabled 当前版本。
2. 保留 FMZ 信号层最新交付物 `neutral_regulation_demo_fmz.py`：FMZ 主循环不调用 LLM，真实 LLM 复核由服务器旁路脚本完成。
3. 保留执行层最新交付物 `spm_calendar_protected_short_v1.py`：STRATEGY_VERSION 2.5.0，默认全空跑，尚未正式启用。
4. 纳入审计页面部署资产：静态前端、materializer、Gemini sidecar 脚本、systemd service/timer 和 env example。
5. 新增服务器自检流程脚本：`tools/server_self_check_signal_stack.sh`。
6. 更新工程总纲、资产索引、备份 README、版本 JSON 和脱敏说明。

## 服务器运行基线

| 项 | 当前路径/服务 |
| --- | --- |
| FMZ JSONL | `/home/bitnami/fmz2/logs/storage/668422/demo/logs/signal_review.jsonl` |
| 审计页面根目录 | `/opt/signal-audit` |
| 审计工具目录 | `/opt/signal-audit-tools` |
| LLM sidecar | `/opt/signal-audit-tools/signal_llm_reviews.jsonl` |
| LLM key 配置 | `/etc/signal-audit/llm.env` |
| GEX key 配置 | `/etc/gexmonitorapi.env` |
| 审计 materializer | `signal-audit-materialize.timer/service` |
| LLM 复核 | `signal-audit-llm-review.timer/service` |
| GEX API | `gexmonitorapi.service` |

## 自检命令

```bash
cd /opt/repos/xxproject
bash tools/server_self_check_signal_stack.sh
sudo bash tools/server_self_check_signal_stack.sh --run-oneshots
```

## 脱敏要求

本轮推送前必须确认没有真实 `GEMINI_API_KEY`、`API_TOKEN`、`.env`、生产 JSONL、缓存或私钥进入仓库。
