# 备份推送记录：2026-06-19 r2.2

备份版本：`NRD-XXPROJECT-BACKUP-2026.06.19-r2.2`

## 本轮目的

r2.2 是一次收纳样式与中文文档惯例对齐，不改变运行逻辑。重点是把 `05_GEX监控API_数据增强接口/` 和 `deploy/signal_audit/` 从“独立服务/部署目录”整理成和 00-04 一样可阅读、可索引、可审计的工程模块。

## 本轮处理

1. 新增 `00_总纲/中性回路工程总纲_v2026.06.19-r2.2.md`。
2. 将根 README、`README_XXPROJECT_BACKUP.md`、`BACKUP_VERSION.json`、99 索引升级到 r2.2。
3. 给 GEX API 新增 `因子文档/`，拆出 GEX 总览、GEX board、Gamma 价位、volatility、flow、rank 语义。
4. 给 GEX API 新增 `docs/README.md`，并把旧 `readme.txt` 标记为历史计划稿。
5. 给审计部署新增 `因子文档/`、`docs/审计卡片语义.md`、`frontend/README.md`、`frontend/VERSION.json`。
6. 保留所有源码、测试、systemd、静态资产原路径，不移动运行文件。

## 不改变的边界

- FMZ 信号层仍是 v1.3.0，只读观察，不在主循环调用 LLM。
- GEX API 仍是 rank-enabled `/v1/info`，本轮只补文档收纳。
- 审计前端仍读取 materializer 输出的 `signal_cards/`。
- LLM sidecar 仍在策略外运行，不改变系统信号和交易许可。
- 执行层仍默认全交易门关闭。

## 验收口径

- r2.2 文档入口全部存在。
- `BACKUP_VERSION.json` 和 `deploy/signal_audit/frontend/VERSION.json` 可解析。
- GEX API 和审计部署的 `因子文档/` 能从 99 索引直达。
- 旧接口草稿中的早期 GEX Monitor 专用端点不再作为当前口径出现。
- 本轮推送到 `xxproject/main`。
