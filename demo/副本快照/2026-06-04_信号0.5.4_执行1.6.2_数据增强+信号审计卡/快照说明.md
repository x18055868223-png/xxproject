# 快照说明 · 2026-06-04

> 信号层 `neutral_regulation_demo_fmz_v0.5.4_2026-06-04.py` — demo 0.5.4 / schema `nrd.schema.v0.5.4`（单文件 mtime 2026-06-04 13:39）
> 执行层 `spm_calendar_protected_short_v1_v1.6.2_2026-06-04.py` — STRATEGY_VERSION 1.6.2 / `ALLOW_TRADING=False`（单文件 mtime 2026-06-04 12:50）

## 本次相对上一封版的变更

### 一、数据增强 gexmonitorapi（A+B 范围）
- **信号·GGR 安全门**：用干净 `total_net_gex`/`market_state`/`magnet` 硬化原"尽力而为"读取（只下调、不上调）。
- **信号·面板+日志**：新增 gex_info 上下文（`iv_rv`/`dvol`/`pcr`/term/flow/market_state/vol_trigger），**不投票、不进 EDB**。
- **VRP**：独立 `gex_info_cross_check`（`iv_rv`/term 交叉校验，`applied_to_gate=False`，**不构成第二道权利金门**）。
- **对冲**：`GGR_ADVERSE` 用 `market_state`/`net_gex`/`volatility_trigger` 硬化（**post-risk only**，风险未抬升不凭空造险）。
- **执行**：`legsel_wall_proximity` 影子避让（**出建议不进排序**，选腿仍纯 delta，不复活 KPF）。

### 二、信号审计卡 Signal Review Card v1.0
- `NR_REPAIR_CONFIRMED` 时落**全量审计卡**：因子截面 + EDB 推理链路（按贡献）+ 冲突比例/原因 + 阻断项/内容 + 置信分解 + 最终结论。
- **三态渲染**：状态栏 Style A 分区面板（替换旧 signal_events 栏，面板 id 仍 `signal_events`）/ FMZ `@` 推送的阅读优化中文综述（Layout 1 分段叙述）/ `signal_review.jsonl` 全量 + 信号桥 digest。
- EDB 仅新增 `confidence_decomposition` 展示字段，**方向/置信/分类阈值零改动**。

## 不变量
软/可降级永不硬门 · GGR 只下调 · VRP 不构成第二道权利金门 · 不复活 KPF · 推送默认关 · 审计卡只读不改决策 · gex_info 缺失逐字节回退既有行为。

## 验证（全绿）
- 信号：`static_validate_demo.ps1` / `runtime_check_demo.ps1` / `tools/gex_info_check.py` / `tools/signal_review_check.py`。
- 执行/对冲：`build_bundle.py --check` / `tests/run_all.py`（67 通过）。
- VRP：`python -m unittest`（31 通过）。
