# 信号层 v1.2.0 验收记录

> 日期：2026-06-17　|　版本：demo **1.2.0** / schema `nrd.schema.v1.0.0`
> 运行环境：Windows 10 / Python 3.12
> 范围：推送正文重设计（四审计层）+ 推送自检开关。只改渲染/可观测，不改方向/EDB/阻断/执行契约。

---

## A. 静态验收（doc §9.1）
- 推送含固定四标题：`【一、背景层：窗口与市场底色】` / `【二、修正层：期权与安全门】` / `【三、论证层：EDB 合成账本】` / `【四、冲突层：为什么…】`（动态后缀：可交易/暂不交易/被一票否决）。✅
- 每条推送只调用一次 `fmz_push()`；末尾仍由 `fmz_push()` 追加 ` @`。✅
- 无 Markdown 表格、无 HTML、不依赖等宽字体。✅
- 首屏（头部 5 行）短，先答 结论/置信/一句话为何不交易；数据行最长 87 字符（修正层 Gamma 含真实 regime 枚举，属 doc 允许的「数据行可略长」）。✅

## B. 内容验收（doc §9.2，`signal_review_check.py` 自动断言）
四类样例均断言四层标题 + `TMV`/`Macro`/`Gamma`/`SRD`/`confidence =`/`冲突`/`阻断`/`JSONL signal_review` + 未校准提示：
- `HARD_VETO`（MACRO_BLOCKING）：含「为什么被一票否决」「宏观硬阻断」「macro_score」置信归零过程。✅
- `TRADE_SUPPORT_STRONG`：含「为什么可交易」「看涨信用价差」。✅
- 窗口未开（NO_TRADE_BLOCKED）：含「为什么暂不交易」。✅
- 样例卡 `build_sample_review_card`（WAIT_CONFIRMATION）：四层齐全、反向证据(宏观/期权偏斜)入冲突层、含 VOLQ/DXY 组件、未校准提示。✅

## C. 推送自检开关（用户新增需求）
`signal_review_push_test`（默认 False）。自动验证（`_v12_verify` 临时脚本，已清理）：
- True → 启动推送**恰一次**；再次调用幂等不重推。✅
- False/缺省 → 完全静默。✅
- 推送带「【推送自检·非真实信号】」横幅；四层标题齐全；末尾 ` @`；含 `confidence =`。✅

## D. 综合验收（6/6 全绿）
| # | 检查 | 结果 |
|---|---|---|
| 1 | `static_validate_demo.ps1` | Static validation passed. |
| 2 | `runtime_check_demo.ps1` | Runtime validation passed. |
| 3 | `signal_review_check.py`（升级四类断言 + 样例卡） | PASS |
| 4 | `gex_info_check.py` | PASS |
| 5 | `greeks_freshness_check.py` | PASS |
| 6 | `v1_1_acceptance_check.py`（push token/fallback/calibration_state） | PASS |
- `build_fmz_single.ps1 -Check` → in sync；交付副本 == canonical（hash 一致）；`demo_version 1.2.0` / schema `v1.0.0`。

## E. 操作员验收（doc §9.3）——样例推送可直接回答 8 问
渲染样例（`signal_review_push_test` / 样例卡）：

```text
【中性回路·信号审计 v1.2】BTC #3272
时间 2026-06-17 18:29:00 ｜ 现价 63,339.96
结论 方向中性 / WAIT_CONFIRMATION / 暂不交易、等待确认
置信 38/100 未校准 ｜ EDB +0.18 ｜ 一致 62% ｜ 覆盖 83%
一句话：窗口已开；量价主干、主动流×价(4h)偏向、宏观、期权偏斜反向，Gamma 降信；证据冲突未消解，等待确认。

【一、背景层：窗口与市场底色】
NR 窗口：NR_REPAIR_CONFIRMED｜位移 向下｜峰值 -0.92｜合并 4
Anchor：score 72｜ND -0.31
TMV：blend +0.42 多｜24h +0.31｜48h +0.49｜冲突 否
Macro：score +0.23 Mild Headwind｜conf 1.00｜VOLQ +210bps / DXY +35bps / US10Y +8bps

【二、修正层：期权与安全门】
Gamma：POSITIVE_GAMMA_PINNING｜net_gamma +12.4M｜pin 64000｜flip 62800｜ggr_mult 0.80｜veto 否
SRD：vote -0.20 空｜RR -0.06｜Δrr -0.03｜data OK
Funding：last +0.0120%｜effect mild_crowded｜hard 否

【三、论证层：EDB 合成账本】
score_raw +0.24 → gamma 0.80 → EDB +0.18
confidence = strength 0.44 × agreement 0.62 × coverage 0.83 × gamma 0.80 = 38
证据贡献：
  + TMV        vote +1.00｜w 0.34｜贡献 +118%｜量价主干·24h/48h 同向
  - MACRO      vote -0.50｜w 0.16｜贡献 -28%｜宏观·Mild Headwind
  + CVD_4h     vote +0.35｜w 0.18｜贡献 +22%｜主动流×价(4h)·BUY_CONFIRMS_UP
  - SRD        vote -0.20｜w 0.18｜贡献 -13%｜期权偏斜·RR -0.06

【四、冲突层：为什么暂不交易】
冲突 MATERIAL｜ratio 38%
同向：量价主干、主动流×价(4h)
反向：宏观(-0.50,w0.16)、期权偏斜(-0.20,w0.18)
剔除/降权：主动流×价(12h)、资金费反身、Gamma空间钉 未计入(权重0/数据不足/不给票)；缺失项不重分配权重
阻断：SOFT_GATE 证据未收敛/置信未达档，等待确认｜解除条件：待方向证据收敛、置信达档(≥50)

【复盘索引】
JSONL signal_review #3272
字段：factor_cross_section / reasoning / conflict / blocking
说明：置信为未校准证据质量分，不代表真实胜率或盈亏概率。
```

1 可交易？→ 否（WAIT_CONFIRMATION）。2 最大同向？→ 量价主干 TMV(+118%)。3 最大反向？→ 宏观 MACRO(-28%)。4 Gamma？→ 降信(ggr_mult 0.80，非 veto)。5 Macro 组件？→ VOLQ+210/DXY+35/US10Y+8 bps。6 为何等？→ 置信 38<50、反向未消解。7 解除条件？→ 方向证据收敛、置信 ≥50。8 卡号？→ #3272。

## F. 结论
v1.2.0 **通过工程验收**：推送可读性/审计性达标，自检开关可用。仍为只读观察、工程阶段，非"策略已验证"。
> 注：FMZ 实盘 `@` 推送的真实投递仍需用户在 App/邮箱侧用 `signal_review_push_test=True` 实测确认（离线仅能验证正文以 ` @` 结尾）。
