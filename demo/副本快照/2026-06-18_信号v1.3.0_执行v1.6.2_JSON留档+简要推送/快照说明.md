# 快照说明 · 2026-06-18 · 信号 v1.3.0 / 执行 v1.6.2（JSON 留档 + 简要推送）

## 版本
| 文件 | 版本 | 行数 |
|---|---|---|
| `neutral_regulation_demo_fmz.py` | demo **1.3.0** / nrd schema `v1.0.0`（不变） | 9012 |
| `spm_calendar_protected_short_v1.py` | STRATEGY **1.6.2**（未改） | 2136 |

## 背景：紧凑体仍被截断
v1.2.1 的紧凑推送（~324 字符）实盘**仍被 FMZ 截断**（邮件在「冲突MATERI」处断）。证明 FMZ `@` 推送上限远低于 324。结论：**推送不能承载全量**，必须本地留档全量、推送只发简要。

## v1.3.0：采纳《信号审计 JSON、FMZ 推送与静态 Web 标准 v1.0》
（`docs/信号审计JSON推送与静态Web标准_v1.0.md`）。**只改留档/推送/可观测，不改方向/EDB/阻断/执行契约；nrd schema 仍 v1.0.0。**

- **全量本地 JSON 留档（唯一事实源）**：新增 `signal_review.build_audit_record(card, config)`，把审计卡映射为 **v1.0 标准记录**（13 个顶层分区：`schema/identity/quality/market_context/decision/display_layers/signal_window/blocking/reasoning/conflict/factor_cross_section/delivery/integrity`），单行写入 `signal_review.jsonl`。缺失=null + `quality.degraded_sources`（不以 0 冒充）；`integrity.record_hash`=sha256；证据带 `source_ref`/`effective_weight`；`display_layers` 只存摘要 + `source_refs`，数值仍引用原始字段。
- **FMZ 只推简要版**：新增 `render_push_brief(card, config)`，**单行 ≤160 字符**，决策前置、审计指针置尾（§6.3）：
  `【信号】BTC #3272 中性/等待确认 强度44 置信38未校准 冲突38% 同TMV+1.00,CVD+0.35 反MACRO-0.50,SRD-0.20 详见FMZ Log #3272`（96 字符）。
  配 `audit_static_base_url` 时末尾改 `审计:<base>/c/<id>`。
- **运行时接线**：`main._emit_signal_review_card` 先 `build_audit_record`→写 jsonl（先落盘），再 `render_push_brief`→`@` 推送；推送 try/except 降级简讯不吞；`build_audit_record` 内嵌简要用 `_safe_brief`（简要异常不破坏记录）。自检 `signal_review_push_test` 改推简要版（带「【推送自检·非真实信号】」前缀）。
- **旧文本渲染器保留但运行时不再使用**：`render_review_card_push`（四层/紧凑）仍在、仍被 check 覆盖，待前端落地后再清理。

## 流程目录 + 两份审计样例（待你确认）
本工程新增 `audit_archive/`（[README](../../../audit_archive/README.md)），由 `tools/audit_sample_gen.py` 用合成样例卡生成：
1. **全量本地 JSON**：`audit_archive/source/signal_review.jsonl`（压缩单行）+ `audit_archive/cards/2026/06/18/<card_id>.json`（易读全量）。
2. **FMZ 简要推送**：`audit_archive/samples/fmz_push_brief.txt`。
另含 `public/data/index.json`（检索索引）、`public/data/cards/<id>.json`、`public/index.html`（占位，**前端样式待你打磨**）、`state/export_checkpoint.json`。

## 验证（全绿）
- 综合 6/6：static_validate / runtime_check / signal_review_check（新增 `_check_audit_record_and_brief`：13 分区齐全 + JSON 单行可解析 + 证据 source_ref + 简要 ≤160 单行 + 校准可见 + 静态链接开关）/ gex_info_check / greeks_freshness_check / v1_1_acceptance（fallback 改打 render_push_brief；校准在 brief+record 可见）。
- `build -Check` in sync；交付副本==canonical；`demo_version 1.3.0`。

## 部署 / 使用
- FMZ 整份粘贴。验证推送：`signal_review_push_test=True` 重启 → 收一条简要自检 → 改回 False。
- 正常推送：`signal_review_push_enabled=True`（出信号推一条简要；全量在 `demo/logs/signal_review.jsonl`）。
- 静态站点 base：设 `audit_static_base_url` 后简要末尾给审计链接。
- **下一步（用户）**：基于 `audit_archive/public/`（数据契约：先 `index.json` 后单卡 JSON）打磨静态前端样式。
