# 信号层 v1.1.0 验收记录

> 日期：2026-06-05　|　版本：demo **1.1.0** / schema `nrd.schema.v1.0.0`
> 运行环境：Windows 10 / Python 3.12（`...\Python312\python.exe`）
> 范围：本记录**单独验收 v1.1.0 的两个新增点**——v1.0.0 全绿证据仍继承自 2026-06-04，本记录补齐 v1.1 增量的针对性验收 + 最终综合验收。
> 权威工件：`中性回路 - opus4.8/`（canonical 多文件 + rebuild 单文件）；交付副本 `整合工程/demo/最新交付物/`（与 canonical 字节一致）。

---

## A. v1.1 增量针对性验收（新增 `tools/v1_1_acceptance_check.py`）

驱动**真实代码路径**，非桩替身。逐点结果：

| # | 验收点 | 断言内容 | 结果 |
|---|---|---|---|
| 1 | **推送函数静态 + 功能检查** | ①功能：monkeypatch `Log` 捕获，`fmz_push("multi\nline")` 输出以空格分隔的 ` @` 结尾，且非裸 `@` 结尾；②源码：`demo/utils.py` 含 `str(text) + " @"`、不含旧的 `str(text) + "@"` 拼接 | **PASS** |
| 2 | **审计卡 fallback** | monkeypatch `main.render_review_card_push` 抛异常 → 以桩对象调用**真实** `DemoRuntime._emit_signal_review_card`：降级推送含「降级简讯」+ 卡号 `acc1` + 以 ` @` 结尾；且 `recorder.write("signal_review", ...)` 仍在推送前发生（JSONL 不丢） | **PASS** |
| 3 | **calibration_state 可见性 + 配置开关** | `UNCALIBRATED` 同时出现在 EDB payload / `card.conclusion` / `card_digest` / 推送综述（含「未校准」「证据后验质量分」）；把 `config.edb_calibration_state` 翻 `CALIBRATED` 后 EDB 字段随之变、综述「未校准」提示消失（证明非硬编码） | **PASS** |

重跑：`python tools/v1_1_acceptance_check.py` → `v1_1_acceptance_check: PASS`

```
PASS  1 push token         | push ends with ' @' (functional + source); bare '@' concat removed
PASS  2 card fallback       | render exception -> degraded one-liner push with card id + ' @'; JSONL still written
PASS  3 calibration_state   | calibration_state UNCALIBRATED in edb/card/digest/push; CALIBRATED toggle clears notice
```

---

## B. 最终综合验收（v1.1 新增 + v1.0 继承全套）

| # | 检查 | 命令 | 结果 |
|---|---|---|---|
| 1 | 静态校验（FMZ 同步 + 交付摘要 + preflight + 无未完成标记 + schema v1.0.0） | `tools/static_validate_demo.ps1` | **Static validation passed.** |
| 2 | 运行时离线全链 smoke（契约 ok + 阻断态机器侧空） | `tools/runtime_check_demo.ps1` | **Runtime validation passed.** |
| 3 | 信号审计卡（阻断/可交易/窗口未开三例 + 推送 token） | `tools/signal_review_check.py` | **PASS** |
| 4 | gex_info 数据增强（降级/质量标签） | `tools/gex_info_check.py` | **PASS** |
| 5 | Greeks 新鲜度（SRD STALE / GGR 丢钉保 flip） | `tools/greeks_freshness_check.py` | **PASS** |
| 6 | **v1.1 增量验收**（A 节三点） | `tools/v1_1_acceptance_check.py` | **PASS** |

### 构建同步 + 版本核对
- `build_fmz_single.ps1 -Check` → **FMZ single file is in sync.**
- 交付副本 == canonical 单文件：**True**（`Get-FileHash` 一致）
- `"demo_version": "1.1.0"` / `"schema_version": "nrd.schema.v1.0.0"`（schema 不变——`calibration_state` 为新增非破坏字段）

---

## C. 验收结论
v1.1.0 **通过工程验收**：两个新增点（推送修复、置信未校准标注）均有针对性、可重跑的证据并驱动真实代码路径；全套综合验收 6/6 全绿；交付单文件与权威构建字节一致。语义仍为**工程阶段**（只读/空跑安全、标定后置），**非"策略已验证"**。

## D. 遗留 / 待确认（不阻断本次验收）
1. **FMZ 实盘 `@` 推送**：离线 shim 仅能验证正文以 ` @` 结尾（已 PASS），无法验证 FMZ 平台实盘解析。需实盘出一次信号最终确认推送到达。
2. **`01_信号层_中性回路/交付物快照/` 旧残留**：该层级快照单文件为 9233 行旧非-canonical 版本（仍含旧 `text+"@"`），早于 1.0.0 reconverge 即与权威分叉，本次未动；权威可部署仍以 `demo/最新交付物/` 为准。是否用 1.1.0 覆盖对齐待用户定夺。
