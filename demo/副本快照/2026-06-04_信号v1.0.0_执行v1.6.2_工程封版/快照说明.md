# 快照说明 · 2026-06-04 · 信号 v1.0.0 / 执行 v1.6.2（工程阶段封版）

## 版本与修改时间
| 文件 | 版本 | 修改时间(本机) | 行数 |
|---|---|---|---|
| `neutral_regulation_demo_fmz.py` | demo **1.0.0** / schema `nrd.schema.v1.0.0` | 2026-06-04 19:34 | 8342 |
| `spm_calendar_protected_short_v1.py` | STRATEGY_VERSION **1.6.2** | 2026-06-04 12:50 | 2136 |

## 本次里程碑
信号层基于《信号层全链路中文语义档案》全链路审计，封 **v1.0.0（工程阶段）**：
- **工程封版语义**：代码正确 + 集成 + 全链路全绿 + 只读/空跑安全。**非"策略已验证"**——标定阈值仍 PLACEHOLDER（`nr_threshold_profile=relaxed_test`）、`ALLOW_TRADING=False`、闸 B 实盘仍待 2–4 周多区制实盘复观（遵 HANDOFF 红线）。
- **本轮真实缺口修复（observer 范围）**：
  - A 机器侧歧义硬化：EDB 阻断/等待 → 机器侧置空（signal=Neutral / strategy_code=none / strategy_type=None / `DecisionSnapshot.side`=none）+ `allow_downstream_evaluation` + 契约红灯断言；TMV legacy 预览移入 `preview_signal`/`preview_strategy_type`。
  - B Greeks 新鲜度进 SRD/GGR：取数失败不刷成功时间；过期 → SRD `data_state=STALE`（EDB 丢票）、GGR 丢弃 stale 钉/net-gamma 但保留 flip 安全门。
  - C GEX `STALE` 预警带（stale_ms<expired_ms）+ 文档对齐 v1.0.0（SCHEMAS/VALIDATION/signal_build README）。
- **此前已含**：gex_info 数据增强（GGR 硬化 + 面板 + VRP 交叉校验）、信号审计卡（状态栏 Style-A / 推送综述 / JSONL + 桥 digest）。
- **押后 Phase 2（非 observer 阻断）**：SignalEvidence 桥真实快照映射（读 `factor_snapshot.edb`/`decision.strategy_recommendation` 嵌套位）+ 运行时导出 + 本机总线 + 消费闭环；JsonlRecorder 轮转/校验。

## 验证（全绿）
- 信号：`static_validate_demo.ps1`（FMZ 同步 + 交付摘要 + 无未完成标记 + schema v1.0.0）/ `runtime_check_demo.ps1`（离线全链 smoke，阻断态机器侧空 + 契约 ok）/ `tools/gex_info_check.py` / `tools/signal_review_check.py` / `tools/greeks_freshness_check.py`。
- 执行：`build_bundle.py --check`（语法 + 名称解析）/ `tests/run_all.py`（67 通过）。

## 部署
FMZ 控制台 → Python 策略 → 整份粘贴。信号默认只读；执行默认空跑（`ALLOW_TRADING=False`）。
可选：`gex_info_token`（或 `NRD_GEX_INFO_TOKEN`）+ 公网可达 `gex_info_base_url` 启用数据增强；`signal_review_push_enabled=True` 启用审计卡 `@` 推送/邮件。
