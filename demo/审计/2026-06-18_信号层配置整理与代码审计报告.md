# 2026-06-18 信号层配置整理与代码审计报告

## 审计对象

- 当前最新交付物：`demo/最新交付物/neutral_regulation_demo_fmz.py`
- 配置合同测试：`demo/tests/test_latest_delivery_config_contract.py`
- 审计范围：用户配置集中化、GEX `/v1/info` 默认接入、信号审计 JSONL 与 FMZ 短推路径、只读边界、明显部署风险。

## 结论

当前最新交付物已完成配置层整理：

- `CONFIG` 顶部新增“用户配置区”，把部署时真正需要改的标的、运行、日志、HTTP、Binance、Macro、Deribit、GEX、审计输出和日志目录集中到一处。
- 新增 `USER_CONFIG_KEYS` 和 `USER_CONFIG_DOC_CN`，用机器可检查清单约束用户配置面，并为每个用户配置项提供中文语义说明。
- GEX 增强接口默认改为策略服务器根地址 `http://13.231.16.198:8000`，token 已写入当前交付物配置。报告不复写完整 token。
- 新增 `_gex_info_endpoint()`，允许 `gex_info_base_url` 填服务器根地址或完整 `/v1/info`，实际请求统一落到 `/v1/info`。
- 信号审计 JSONL 写入和 FMZ 短推路径仍保留原设计：全量写 `signal_review.jsonl`，FMZ 只发短简讯，自检开关默认关闭。

未发现阻塞级代码问题。

## 关键证据

- 用户配置区起始：`neutral_regulation_demo_fmz.py:153`
- GEX 默认配置：`neutral_regulation_demo_fmz.py:228-229`
- 审计输出默认配置：`neutral_regulation_demo_fmz.py:237-242`
- 用户配置清单：`neutral_regulation_demo_fmz.py:484`
- 中文语义说明：`neutral_regulation_demo_fmz.py:543`
- GEX endpoint 归一化：`neutral_regulation_demo_fmz.py:5901`
- GEX 适配器调用归一化 endpoint：`neutral_regulation_demo_fmz.py:5937`
- 真实信号审计 JSONL 与短推：`neutral_regulation_demo_fmz.py:9296-9314`
- 推送自检：`neutral_regulation_demo_fmz.py:9317-9337`

## 验证结果

已执行并通过：

```text
C:\Users\Xu\AppData\Local\Programs\Python\Python312\python.exe demo/tests/test_latest_delivery_config_contract.py
latest_delivery_config_contract: PASS
```

```text
C:\Users\Xu\AppData\Local\Programs\Python\Python312\python.exe -m py_compile "demo/最新交付物/neutral_regulation_demo_fmz.py"
exit code 0
```

```text
C:\Users\Xu\AppData\Local\Programs\Python\Python312\python.exe demo/tests/test_signal_audit_frontend_contract.py
signal_audit_frontend_contract: PASS
```

```text
C:\Users\Xu\AppData\Local\Programs\Python\Python312\python.exe demo/tests/test_signal_review_push_self_test_json.py
signal_review_push_self_test_json: PASS
```

```text
C:\Users\Xu\AppData\Local\Programs\Python\Python312\python.exe demo/tests/test_signal_cards_materializer.py
signal_cards_materializer: PASS
```

结构化 AST 审计：

```text
{'config_keys': 229, 'duplicate_keys': [], 'user_keys': 56, 'missing_in_config': [], 'missing_docs': [], 'extra_docs': []}
```

最新交付物单文件静态扫描：

```text
Select-String -LiteralPath 'demo\最新交付物\neutral_regulation_demo_fmz.py' -Pattern
'127\.0\.0\.1:8000','gex_info_token": ""','render_review_card_push','signal_review_push_compact'

无输出
```

## 审计发现

### P0/P1 阻塞问题

未发现。

### P2 风险与边界

1. GEX token 已按需求写入交付物配置，但这意味着该文件不适合直接推送到公开 GitHub 仓库。若后续要把信号层也纳入 Git 流程，应使用私有仓库，或改为 FMZ 参数覆盖/部署时注入 token。

2. 历史快照和旧文档中仍保留 `127.0.0.1:8000/v1/info`、旧长文渲染器说明和历史函数。这些是归档证据，不代表当前最新交付物。部署 FMZ 时应只取 `demo/最新交付物/neutral_regulation_demo_fmz.py`。

3. `audit_static_base_url` 仍保持空值是正确状态。当前静态审计前端尚未启用 `/c/<short_id>` 深链，提前打开会让短推产生不可达链接。

## 运行建议

- 正常等待真信号时保持：
  - `signal_review_enabled = True`
  - `signal_review_push_test = False`
  - `audit_static_base_url = ""`
- 如需真信号 FMZ 短推，才打开：
  - `signal_review_push_enabled = True`
- 部署后第一轮检查：
  - FMZ 日志确认 GEX 数据源不再显示 `GEX_INFO_NOT_CONFIGURED`
  - 服务器端查看 `signal_review.jsonl` 是否追加
  - 审计页面刷新后确认新卡片数量增加且 `Record` 不再是 `SYNTHETIC`
