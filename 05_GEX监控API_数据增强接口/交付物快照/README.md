# GEX Monitor API 交付物快照说明

> 当前模块口径（r2.2 / 2026-06-19）：本模块是仍在运行的独立 FastAPI 服务。为避免破坏 Python 包、pytest、systemd 与服务器部署路径，源码仍保留在模块根目录的 `src/`、`tests/`、`deploy/`，没有搬入本目录。

本目录只作为与 00-04 模块一致的“快照说明”占位：

- 当前可部署源码：`../src/gexmonitorapi/`
- 当前测试：`../tests/`
- 当前部署资产：`../deploy/`
- 当前中文语义入口：`../因子文档/00_GEX监控API总览.md`

r2.2 的收纳重点是新增因子文档和索引，不改变运行文件位置。
