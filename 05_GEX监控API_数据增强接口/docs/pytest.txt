import requests
BASE = "http://13.231.16.198:8000"
H = {"Authorization": "Bearer <REDACTED_GEX_INFO_TOKEN>"}
info = requests.get(f"{BASE}/v1/info", headers=H, timeout=30).json()
print(info["availability"], info["gex_board"], info["flow"])
# 手动催一轮(一般不用,后台每 10 分钟自动刷新):
# requests.post(f"{BASE}/v1/refresh?section=all", headers=H, timeout=120)