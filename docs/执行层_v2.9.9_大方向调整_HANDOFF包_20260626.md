# 执行层 v2.9.9 大方向调整 HANDOFF 包

> 生成日期：2026-06-26
> 当前状态：执行层 v2.9.9 已冻结为可复用资产库；用户后续将调整执行层大方向，当前停止继续推进新逻辑。
> 安全边界：不得标记 `DRY_RUN_PASSED`；不得打开真实交易门；不得把本地测试通过等同于 FMZ 空跑或交易所只读验收通过。

## 1. 下一窗口读档顺序

1. `docs/执行层新版本重建_阅读与资产索引_20260626.md`
2. `PROJECT_MEMORY.md`
3. `docs/执行层_v2.9.9_当前资产清单_暂停点.md`
4. `docs/执行层_HANDOFF_下一窗口.md`
5. `docs/执行层完整说明_v2.1.md`
6. `docs/执行层重构_v3_进度.md`
7. `demo/最新交付物/README.md`

其中 `docs/执行层_v2.9.9_当前资产清单_暂停点.md` 是当前暂停点的最短入口；本文件用于给新窗口直接接手和复述边界。

## 2. 当前 canonical 资产

- 源码基座：`demo/execution_build/realsrc/src/`
- 测试入口：`demo/execution_build/realsrc/tests/run_all.py`
- 源 bundle：`demo/execution_build/realsrc/spm_calendar_protected_short_v1.py`
- FMZ 最新交付物：`demo/最新交付物/spm_calendar_protected_short_v1.py`
- 快照：`demo/副本快照/2026-06-26_执行v2.9.9_双包接收血缘硬化/`
- 当前版本：`STRATEGY_VERSION = "2.9.9"`
- 当前 SHA256：`9DBAB3BF58F0F502E172FB3AD3EEB74B35974C677B4879DF6B7722C31EB8A34E`

默认安全门仍为关闭：`ALLOW_ENTRY_TRADING=False`、`ALLOW_EXIT_TRADING=False`、`ALLOW_HEDGE_TRADING=False`、`KILL_NEW_RISK=False`、`EMERGENCY_REDUCE_ONLY=False`、`DRY_RUN_PASSED=False`。

## 3. 已实现资产速记

- 安全门控：entry/exit/hedge/kill/emergency 拆门，`DRY_RUN_PASSED=False` 会硬阻断真实交易门。
- 命令与交互：`GetCommand`、短确认码、授权/撤销/急停/恢复、幂等账本、状态栏控制台。
- 计划与审批：垂直价差唯一、推荐库、确认码血缘、批准 TTL、锁定方案、信号血缘变化清锁与重建。
- 信号接收：兼容当前 `signal_review.jsonl`，也兼容未来 `EntryCandidatePackage + SignalContextUpdate`；FILE 双包入口会读取同目录 `context_latest.json`，且 context 缺 `episode_id/source_snapshot_hash` 时 fail-closed。
- VRP 与预提交：完整 IV/RV `market_context` 下做计划轮 VRP 预过滤和预提交复核；缺上下文不伪造通过。
- 执行可行性：进入计划过滤/排序、确认码质量血缘和预提交复核。
- 开仓活动：跨轮 maker entry campaign、信用底线、保护腿先成交、放弃后保护腿-only 残值进入管理。
- 持仓管理：唯一持仓快照 `_POSITION_KEY`、止盈资格、退出活动、风险退出授权预算、保护腿回收、CLOSED 归档。
- 风险与对冲：入场风险锚、持仓后 EDB/GGR 连续性、`hedge_risk.evaluate_position_risk`、结构净 delta 对冲、Deribit/Binance 对冲适配。
- 恢复与未知订单：启动恢复、真实活动订单查询、未知订单 fail-closed、管理期相关活动订单 `MANAGE_IN_FLIGHT`。
- 行情复用：同一持仓管理轮复用 quote 快照；缺 quote 不缓存，允许同轮后续模块重试。

这些资产后续应作为“可复用组件库”和“回滚点”，不要默认继续线性堆 v2.10。

## 4. 验证证据

- `demo/execution_build/realsrc/tests/run_all.py`：264 passed / 0 failed。
- `demo/execution_build/realsrc/build_bundle.py --check`：通过。
- 源 bundle 与最新交付物 SHA256 一致：`9DBAB3BF58F0F502E172FB3AD3EEB74B35974C677B4879DF6B7722C31EB8A34E`。
- 最新交付物和源 bundle Python 3.12 `py_compile` 通过。
- reviewer 复核无阻断发现；其提出的 FILE 双包缺 hash 负向集成测试已补入 `test_receive_file_entry_candidate_blocks_adjacent_context_missing_hash`。

注意：这些都是本地证据，不是 FMZ 真机空跑通过。

## 5. 未完成项与红线

- 未完成 FMZ 真机确认码空跑。
- 未完成交易所订单/持仓只读核验。
- 未完成信号层原生双包生产者验收。
- 未完成阈值标定和小额实盘配置验收。
- 暂不软化 `spread_ok`、`vrp_rechecked` 或其他经济性门控；这些问题等待新的整体执行层设计后再判断。
- 不继续做小修小补式推进；下一步应先做大方向设计、迁移路径和资产取舍。
- 信号层不在本轮改动范围，除非用户明确要求。

## 6. 新 Codex 窗口 Prompt

```text
请先阅读：
1. docs/执行层新版本重建_阅读与资产索引_20260626.md
2. PROJECT_MEMORY.md
3. docs/执行层_v2.9.9_大方向调整_HANDOFF包_20260626.md
4. docs/执行层_v2.9.9_当前资产清单_暂停点.md
5. docs/执行层_HANDOFF_下一窗口.md
6. docs/执行层完整说明_v2.1.md
7. docs/执行层重构_v3_进度.md

当前任务不是继续推进执行层 v2.9.9，而是基于用户新的大方向重新评估和设计执行层。v2.9.9 只作为可复用资产库和回滚点。

当前执行层最新交付物为 demo/最新交付物/spm_calendar_protected_short_v1.py，STRATEGY_VERSION=2.9.9。当前本地证据：tests/run_all.py=264 passed / 0 failed，build_bundle.py --check 通过，源 bundle 与最新交付物 SHA256=9DBAB3BF58F0F502E172FB3AD3EEB74B35974C677B4879DF6B7722C31EB8A34E。

严格红线：
- 不得标记 DRY_RUN_PASSED。
- 不得打开真实交易门。
- 不得声称 FMZ 空跑或交易所只读验收已通过。
- 不得继续线性小补丁式推进 v2.9.9。
- 不得在新设计确认前软化 spread_ok、vrp_rechecked 或其他经济性门控。
- 信号层不在本轮改动范围，除非用户明确要求。

请先复盘当前已实现资产，结合用户新的执行层大方向，输出设计方案、迁移路径、需要保留/废弃/重构的资产清单，以及验证与验收边界。等用户确认后再实施。

当前协作模式：Codex 实现 + Claude 复核 + FMZ 真机验收。
```

## 7. 给 Claude 复核的边界提示

复核重点不是继续找 v2.9.9 的细枝末节，而是确认下一阶段设计没有误用当前资产、没有跨越安全边界、没有把本地测试包装成 FMZ 通过、没有在信号层生产者未更新前提前宣称闭环。
