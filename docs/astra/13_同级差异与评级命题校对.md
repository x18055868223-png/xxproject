# 同级差异与评级命题校对

日期：2026-09-10。状态：真实卡只读调查与后续调整建议，尚未修改评级、Prompt、前端或生产数据。

## 本轮事实

目标卡：`20260910T053958+0800-BTC-nr_1788988008200_UP-acc4`，北京时间05:39:58。从服务器取原生记录、唯一LLM sidecar、物化卡、列表摘要与持久事实包，不追加模型调用。证据副本位于根目录 `.artifacts/astra-side-comparison-20260910/evidence.json`。

- 卡片时点价格78,231.99 USDT；量价与主动流均下行；4小时价格净变化-0.6527%，12小时-1.3667%。窗口净变化不证明期间连续单向运动。
- LLM独立价格倾向已经输出BEARISH；Put与Call均B。列表摘要已有独立价格倾向，最高决策区仅展示量价和主动流倾向，因此用户很难直接看到完整结论。
- 现价距Put墙约1.57%、Call墙约2.26%；Gamma过渡区。较远墙距只描述至该结构参照的距离，不证明墙的约束强度，更不是拟成交两腿的风险缓冲。
- 两侧行动均WATCH；最高摘要却使用“B级侧关注、C级侧普通观察”的固定模板。本卡没有C级侧，这是具体展示缺陷。

## 比同级更重要的错误

Put侧支持理由主要叙述下行压力，反证却写锚带与净Gamma可能缓冲；失效条件是“价格显著上行……Put侧不利推进减弱”。这更像在解释何时推翻看跌观点，而不是何时推翻Put信用价差的适配论证。不能把这一侧的B直接当成与Call B同等可靠的支持，也不能未经修复就擅自改成C或B−。

现有Prompt已要求结构优先与Put关注下行侵入，但字段语义没有明确锁定主张；本地校验主要验证格式、引用、时点、部分语义越界与行动权限，未充分识别支持/反对的命题错位。技术状态OK不等于推理正确。这张卡应保留为原始反例。

Call侧的方向与压力响应解释相对一致，但当前下行不保证未来不反弹，Gamma过渡区也没有独立建立明确约束。因此不能仅因看跌就把Call直接升A。Funding温和正值可作背景，不应单凭非拥挤费率构成有力反证。

## 调整建议（未实施）

1. 固定两侧评审对象：Put评估下行风险是否受到可解释约束，Call评估上行风险是否受到可解释约束；与独立价格方向严格分开。支持、反对、竞争解释、增强与失效条件围绕同一对象。对D/C等低支持等级，分别说明什么增强适配、什么削弱适配，避免“推翻当前评级”与“推翻适配主张”再次混用。
2. 在同一次综合评审内增加两侧相对比较：Put更有依据、Call更有依据、接近、不可比较；要求说明决定差异的事实及何时反转。未知一侧不能因为缺资料自动让另一侧胜出。相对有利不等于达到A或可交易，不单独新增模型调用。
3. 暂不把D–S直接扩展成大量子等级。B+/B−可以作为未来表达同一主等级内证据强弱的备选，但须有稳定口径，不能把看跌机械转成Call+、Put−，也不能用+-修饰掩盖命题错误。若以后采用，主等级仍控制关注与准备含义，所有接口、筛选、权限、历史协议需一起明确；本轮不引入数字分或胜率。
4. 顶部与列表展示卡片时点价格、独立LLM倾向、两侧等级；详情保留量价/主动流作为依据，缺失时明确未评估。价格使用实际单位与来源时点，不写成实时价格；列表复用摘要，不能为了价格提前加载全部卡。相对侧别只有有效原生结果才展示，旧卡不从关键词或方向推算补造。
5. 行动摘要仅描述实际存在的侧别状态，修复两侧B却提到C的模板。保留原评级、原回答及历史完整性，未来新输出版本按兼容约定保存，不自动重跑本卡。

推荐实施次序：命题与正反证校对 → 同次跨侧比较 → 顶部/列表价格与倾向 → 再看子等级是否仍有信息增益。结构、压力与响应共同决定证据，不能回退成方向动量打分。

## 验收要求

- Put反弹改善下行风险时，不能被写成“Put适配失效”；下行风险加剧也不能写成Put支持，除非明确给出压力未传导等可核验机制。
- 覆盖下行但下方约束有效、同级但相对侧别不同、两侧接近、单侧无效、方向与结构冲突；不机械要求两侧异级。
- 新评级不根据最终盈亏调权；保留真实失败响应。主张语义不能仅靠词语黑名单判定，也不宣称确定性代码已能验证全部因果叙述。
- 价格、倾向与评级的列表/详情来源一致，缺失不回退旧信号猜测，旧卡可读；不改变执行权限和自动调用频率。
- 前端调整保持已选中性纸面；本地浏览器确认后再取得本轮发布授权，不在本次调查中直接推送或部署。
