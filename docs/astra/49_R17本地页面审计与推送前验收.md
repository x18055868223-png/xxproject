# R17 本地页面审计与推送前验收

2026-09-23 更新。研究结论仍以[最终交付48](48_R17研究收束与最终交付.md)为准：风险工具有用，业务未成立。本页只记录本地阅读器、审阅包和待推送范围，不重开模型、阈值或交易策略研究。

## 本轮修正

本地页面入口为 `http://127.0.0.1:8884/`，使用正确开发工作树的当前 `deploy/signal_audit/frontend` 源码与封存的两张 2026-09-12 自然 NR 历史卡。旧预览、原卡 JSON 和 R17 研究制品均保留。页面首屏把 R17 当前裁定置于卡时统计及 LLM 意见之前；联合卡顶部标为“联合研究评估／卡时研究意见”，避免把旧风险排序当成已采纳策略。若旧卡误带 `tail_probability_status=available`，建议区仍仅显示“暂不采用”，不输出保护腿突破百分比。统计赔付估计、LLM 文本、模型、FMZ、权限和单次请求规则未改。

新版 `VERSION.json`／页面缓存身份为 `20260922-astra-r17-review`；R17 裁定发生于 09-22，并不表示重新训练或替换运行模型。默认 `signal_cards/` 是自然卡入口；另设的 `?research=joint` 新价格事件影子入口没有本地静态档案，不能拿它代替两张自然卡，也未为展示补造其样例。

## 本地验收与证明上限

- 浏览器实际检查 1440、1280 和 390 宽度，切换两卡、搜索和评级筛选空态、章节跳转、键盘焦点及完整资料下载；所查页面无横向溢出和控制台错误。17:10 卡的 Put 方向建议未采纳，07:12 卡仍是卡时 Call 侧研究意见；两卡建议区的尾部百分比均退出。
- 当前候选相关离线测试：467 通过、1 跳过；旧 v1.5 可选 `hmmlearn` 合成导出测试在原环境缺依赖，随后用既有封存依赖单独跑过。两次运行合计覆盖 468 项测试，不把跳过项记作第一次已通过。前端新增 R17 文案/尾部兜底的回归在其中。当前 13 个兼容脚本逐个通过。
- 兼容脚本首跑曾有 1 项因旧 `VERSION.json` 日期断言失败，原日志保存，严格断言更新后该项重跑通过。没有放宽协议或省略该失败。两名项目子 agent 分别核查预览／提交依赖和独立复审；复审提出的脚本入口漏测已补入直接运行检查。
- 新审阅包为工作树 `.artifacts/astra-r17-local-package-20260922/signal-audit-deploy.zip`，SHA256 `ab097a7efb40e3fee63699867f31704cadf88d3b0a3d64c425cc1f03745bcf74`。53 个条目逐字对应包内文件；页面四项源码与当前工作树、预览一致，两张历史卡及索引与原预览逐字一致。标准库模块导入通过。包内不含 qualified 模型和历史大数据，不是独立重训全集。
- ROOT `.artifacts/astra-commercial-review-v17-20260922` 的原 55 项及闭环 28 项 manifest 全部再次按 hash 核对。运行模型仍为 `portable_qualified/model.json`，SHA256 `71fb23ff9cbb55a789cc1c95862e7e242d8647ce8ac883047d10bded01fd5860`；FMZ1.6.2 文件未变。本地审计不能证明已部署、自然卡实际消费、90 日窗口或经济有效。

审计副本、测试和失败日志、逐文件校验以及候选清单在 ROOT `.artifacts/astra-local-page-audit-20260922/`。旧研究产物和全部失败继续留在原目录。上述本地检查不替代服务器真实卡／FMZ验收。

## 提交前边界

正确工作树是 `.worktrees/astra-signal-rating-v1`，当前 `HEAD=cc37445bed0835fad18aa5e2d8ea28b13513980b`；权威 remote `xxproject` 的同名分支在本次只读查询时仍指向该提交。`origin` 是部署镜像。当前无暂存、提交、推送或部署。本地候选清单逐文件列明 Astra 源码、运行依赖、测试、部署脚手架、研究说明及 hash；旧 `.artifacts`、虚拟环境、大行情、旧包和凭据不在候选内。研究大制品在 ROOT，不会随工作树提交，仓库内文档只保存其身份与校验入口。

本机 `.git/info/exclude` 的 `/docs/` 会隐藏部分新研究文档；得到页面确认之后必须按清单显式纳入，不能仅提交 tracked diff，也不能把整个 `.artifacts` 加入。页面发布门是用户明确确认“当前本地页面可推送”，授权后才对 `xxproject/codex/astra-signal-rating-v1` 提交与推送。服务器部署、FMZ更新和自然卡验证仍是独立状态。
