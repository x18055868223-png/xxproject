# Astra GEX 时间补丁发布与 FMZ 1.6.2 交付回执

2026-09-11，用户审阅8881本地页面后明确要求“推送并完成更新部署和检查”。本轮已发布至xxproject的 `codex/astra-signal-rating-v1`，不合并main。实现提交为 [`fc071fe`](https://github.com/x18055868223-png/xxproject/commit/fc071fe0a15c176ee2a223dd208916f473d07c68)；之后的回执提交只更新文档，不改变已部署程序。

## 已完成的更新

GEX服务原生输出按字段区分的市场观测、上游结果生成和值抓取时间；未知不伪造。事实包2.1.1与Prompt2.2.1已安装，模型输出、摘要和主阅读投影仍为2.2.0。FMZ完整交付为1.6.2，只增加审计时间说明，保留旧信号方向、分数、触发、权限与正常一次综合评审。

[正式页面](http://13.231.16.198/signal-audit/)使用 `20260911-astra-time-patch`。Call／Put墙、Pin、翻转点与价格锚已归入空间约束动力学；关键变化骨架补充TMV、看板净Gamma、主动流等前后对照。补充变化为独立只读投影1.1.0，不冒充原模型已采用的论据，不重评历史卡。

| 层面 | 实际核验 |
|---|---|
| Git | 实现提交 `fc071fe0a15c176ee2a223dd208916f473d07c68` 已推送xxproject，远端SHA一致 |
| 程序 | 18项已安装前端、工具、runner及GEX文件与发布包一致；HTTP内容与服务器文件一致 |
| 原始资料 | 原JSONL历史前缀保留；15张阅读卡原评审及完整性信息未改写 |
| 持久状态 | 24项评审、尝试/响应、预算、排除清单和配置文件更新前后相同；恢复timer后再次核对仍相同 |
| 列表详情 | 15组摘要相同，主展示投影及补充变化投影哈希通过 |
| 变化恢复 | 最新真实卡及3张历史卡各有6项可比补充变化；不满足身份条件的卡保留缺口 |
| GEX | 原生 `gex_time_semantics@1.0.0`，19项字段时间说明；真实检查 `stale=false`、`availability=partial`、cross_check=ok |
| GEX缓存 | 安装期间缓存文件逐项hash不变；历史rank数据原前缀保留，随后正常刷新追加 |
| 系统自检 | 开启SESSION_CONTEXT_REQUIRED、DURABILITY_REQUIRED、SYSTEMD_REQUIRED；按真实历史卡原协议检查，27通过、2提示、0失败 |
| 浏览器 | 正式页1440／1280／390及13个展开区通过；无横向溢出、断链、机器字段或页面错误 |
| 请求负载 | 初始manifest加选中卡及有界预取共4次，展开增加0次；本轮部署新增LLM请求0次，预算计数仍5 |
| 服务 | GEX服务、两个正常审计timer均active/enabled；两个oneshot最近执行成功 |

两项自检提示：本次没有主动发起LLM oneshot；已退出自动路径的独立变化LLM没有最新有效对应结果。它们不是恢复旧调用的理由。刚恢复timer时曾在 `activating/start-pre` 瞬态运行自检而报告2项失败，保留日志；自然运行结束后两个oneshot退出码0，完整自检通过，没有修改校验来隐藏状态。安装日志另有Bitnami既有SysV选项忽略提示，未影响服务。

## FMZ交付与自然验收边界

用户可见文件 `demo/最新交付物/neutral_regulation_demo_fmz.py` 已于 `2026-09-11T18:05:44.415566+08:00` 同步为 **1.6.2**，直接取自上述Git实现提交，逐字节相同。SHA256：`746705ada14cbe8ebf3056bbed29a2cce856c586ac056232b8a1c2139e8d8488`。本地候选验收时的另一个哈希源于CRLF换行；发布交付采用Git规范化字节，代码内容一致。旧1.6.1已备份至本轮本地证据目录 `visible_fmz_before_sync/`。

**服务器更新完成，不代表FMZ实例已替换。** 本次正式页及源JSONL最新真实卡仍为2026-09-10 23:00固定轮，producer1.6.0、评审/Prompt2.1.0。因此本轮服务器自检明确按旧卡原协议进行，没有把它标为1.6.2。源数据尚未提供1.6.2自然卡；需用户替换实例后，核验新卡原生字段时间、近端证据及新版评审。首张跨版本卡可保留比较缺口，同版本后续卡再核对变化链。

本轮没有重新运行前一轮的隔离真实LLM检查，也没有发布模拟卡。未开启A／S持续跟踪，未恢复Codex定时跟进。待用户告知新卡后再做前向验收。

## 备份、回退与接管

- 发布目录：`/opt/signal-audit-releases/astra-fc071fe-20260911`，ACTIVE_ASTRA_DEPLOY已在验证后切换至此目录。
- 备份目录：`/opt/signal-audit-backups/astra-before-fc071fe-20260911`，包含审计代码/阅读资料、原始JSONL、状态与配置及GEX代码/缓存，归档SHA256已核验。
- 发布包SHA256：`74b8ea189ec623ceb9e92f6b3b4955178d2686994cb3b1d4aec3d35c5e2ec2bd`。生产包28项代码/工具文件，明确排除所有signal_cards fixture。
- 人工回退入口：发布目录内 `rollback.sh`。先排空任务，保存更新后全部数据，再恢复旧代码与对应阅读投影；新版原始资料、评审、额度和GEX历史保留，审计timer保持暂停直到兼容确认。未实际在生产演练回退。
- 本地证据：`.artifacts/astra-v221-deploy-20260911/`，包括manifest、服务器校验日志、浏览器截图、状态保护记录、FMZ同步回执。密钥与令牌未写入项目文档。

本轮reviewer独立复核发布资产及脚本，指出的GEX失败恢复与备份失败恢复问题已修复；发布指针按安装、验证、浏览器和自检完成后的顺序切换。主agent核对证据、完成服务器操作、正式页面验收及可见交付同步。上一轮explorer/worker的实现与审查详见[补丁20](20_GEX时间语义补丁与FMZ1.6.2.md)。
