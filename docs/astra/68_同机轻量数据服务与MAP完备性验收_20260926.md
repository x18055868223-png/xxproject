# 同机轻量数据服务与 MAP 完备性验收

本轮范围冻结见[协议67](67_同机KPF轻量生产消费与MAP数据完备性实施冻结_20260926.md)。权威远端为 xxproject，工作分支 codex/astra-signal-rating-v1；只发布独立数据服务程序，不将新工作台页面或 FMZ 2.0.1 本地候选随包部署。

## 已核验事实

服务器资源首次实测为2 CPU、945 MiB物理内存、约398 MiB可用内存、12.6 GiB剩余根盘。原 KPF 本机185日缓存合计20.63 GiB，其中13.46 GiB是解压CSV、7.04 GiB是ZIP双副本；单副本原ZIP的90日平均估算约1.71 GiB。该估算不能代替服务器完整90日实测，极端单日容量可使总量超过5 GiB预算，届时必须阻断。

固定保留原90日/60日证据、跨日100 BTC量柱相位、50 USD内部桶、时间权重、原basin/grade/target代码与源hash。生产只流式累计density，不保存全窗口bars，不安装虚拟环境或PDF/SLRP/replay等研究模块。原算法fixture对照与producer→consumer原生三件契约测试是本地证据，不能代替当前90日真实完整计算。

Bitview官方日期起点修为2009-01-01；严格14日按first_height/day1日界映射per-block profit/loss/height，连续唯一height完整性检查、US Dollars单位和原事件语义检查均保留。sum_24h不可当UTC daily相加。期末realized_cap单列stock，不冒充起点stock、资金流或delta。

真实公开回执 `.artifacts/astra-kpf-light-server-20260926/live_pnl_run02.json` 以13次HTTP得到2026-09-12至09-25完整14日：利润10,132,955,501.63 USD、亏损3,842,175,778.63 USD、亏损份额27.49294945%，期末realized_cap为1,074,891,779,125.33 USD。它证明该数据口径可获取，不证明承保、择时、选边或EV提升。首次SOURCE_TIME_IN_FUTURE失败保留，源可得时钟随后改为真实响应完成时刻；后续服务器回执应再核对完整资格。

FMZ本地 snapshots.jsonl 在本轮服务器基线仍为0 B。旧自动LLM和materializer timer仍活跃，新数据链零LLM不能宣称全生产栈已手动化。借款无授权、GEX期限/原生FMZ门、KPF完整窗等仍须按真实状态披露；缺口不能由模拟展示或安装成功填平。

## 发布与验收记录

当前文档在代码推送前建立；Git提交、包hash、服务器安装、单日实测、timer回执与最终完备性将在实际完成后补记。未填项为未验证，禁止当成已部署/已齐数据或完整窗口通过。
