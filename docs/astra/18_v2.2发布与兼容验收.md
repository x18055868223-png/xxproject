# Astra v2.2 发布与兼容验收

2026-09-11，用户审阅8880当前页面并明确要求“推送并更新”。发布目标为xxproject的`codex/astra-signal-rating-v1`完整工作分支，不合并main。该明确指令是本轮当前页面的发布确认，不要求重复口令确认。

## 核验基线与顺序

- 本地及远端分支基点均为`2310814a53db256f06c8e8730376cdf6a20d3a34`。
- 发布前服务器运行v2.1；最新阅读卡为2026-09-10 23:00固定轮，策略1.6.0，评审/Prompt2.1.0。15张阅读卡、两个正常timer；模型仍为已配置的`deepseek-v4-flash`，本轮不改模型配置。
- 已完成的功能、真实API保留项和阅读验收见[方案17](17_证据校正与建议式辅助闭环_v2.2.md)。最后提示词未再次请求模型，部署不能将这项局限改写为通过。
- 先备份代码、阅读投影、原始资料、配置、持久尝试和预算，暂停审计timer并等待oneshot结束；安装后验证历史资料未改写，再仅物化阅读投影，验收后恢复正常timer。
- 服务器先兼容新旧producer；成功后同步用户可见FMZ1.6.1文件，再由用户替换实例。当前服务器真实1.6.0按其原协议验收，原生1.6.1近端链仍须自然新卡证明。

## 执行回执

Git实现提交为[`fc11787a9576d3b36b9a13756e02133f97892644`](https://github.com/x18055868223-png/xxproject/commit/fc11787a9576d3b36b9a13756e02133f97892644)，已推送工作分支，远端SHA核对一致；后续文档回执提交不改变这个已部署代码提交。

服务器发布目录为`/opt/signal-audit-releases/astra-fc11787-20260911`，ACTIVE指向已更新。[正式审计页](http://13.231.16.198/signal-audit/)使用`20260911-astra-v22-reading`页面。整套备份位于`/opt/signal-audit-backups/astra-before-fc11787-20260911/`；包SHA256为`26e517366f117318928a2dc4aab7f7cc036ef1e8c44d1f22dd360fefa5c938b1`。

| 核验项 | 实际结果 |
| --- | --- |
| 安装保护 | 安装代码前后18个阅读目录文件逐一相等，未被包内旧fixture替换 |
| 原评审与配置 | 23项protected文件不变，包括原sidecar、尝试/响应、预算、历史排除清单及配置 |
| 物化结果 | 15张历史卡原评审与源完整性相等，15组列表/详情投影一致 |
| 已安装程序 | 14项前端/工具/runner资产与发布包一致，HTTP返回与本机文件一致 |
| 协议 | 安装输出及Prompt2.2.0；最新真实卡仍为FMZ1.6.0、原评审/Prompt2.1.0 |
| 自动任务 | 两个审计timer恢复active/enabled；预算仍为5，安装与验收未增加模型请求 |
| 严格兼容自检 | 按当前真实1.6.0/2.1协议，开启SESSION_CONTEXT_REQUIRED、DURABILITY_REQUIRED、SYSTEMD_REQUIRED：27通过、2提示、0失败 |
| 正式浏览器 | 1280与390宽度、13个展开区；无横向溢出、失效锚点或机器字段泄漏 |

两个自检提示分别为本次只读未主动触发任务，以及已退出自动流程的独立变化LLM没有最新对应结果；后者是历史协议保留提示，不恢复旧调用。浏览器捕获一条Chrome扩展自身`crypto.randomUUID`错误，来源是扩展页面，不是审计页脚本；未发现审计页面来源的错误。系统单元语法检查另提示Bitnami旧SysV优先级选项已被忽略，审计服务启动成功。

## FMZ交付已同步，实例待替换

用户可见的`demo/最新交付物/neutral_regulation_demo_fmz.py`现为**1.6.1**，2026-09-11 12:27北京时间同步；直接取自上述Git提交，逐字节一致。SHA256：`2e5524bc2e882a8308de0c8b8a5201d245306a16ffb8566bdf7c7910153a01f3`。旧1.6.0和说明已备份到本地发布资料目录的`visible_fmz_before_sync/`。工作树源文件与Git/可见交付内容一致，原Windows换行不作为业务差异。

**尚未替换用户FMZ实例，未完成原生1.6.1前向验收。** 用户替换后，第一张跨版本卡允许明确比较缺口；需要自然1.6.1卡的近端对象与同版本后续卡证明新数据链。旧卡不批量重评，不将模拟或本地重验当作新模型原生结果，不恢复已取消的Codex定时跟进。

## 回退与后续接管

脚本、校验日志和回执保存在用户可见目录`.artifacts/astra-v22-deploy-20260911/`。`rollback.sh`为显式人工回退入口：先暂停并排空审计任务，校验备份，再保存升级后的完整资料，成套恢复旧代码和旧阅读投影。新版原始数据、评审、attempt和预算保留；回退后timer继续暂停，核对旧代码对新状态的兼容性后才恢复。未在生产实际演练回退，不承诺旧页面立即显示全部新版卡。

explorer只读核对交付资产和相关测试；reviewer独立复核安装、保护及回退脚本；主agent核对证据、完成Git发布、服务器安装、实际数据对比、浏览器检查和本地交付同步。发布验收证明代码部署与历史兼容，不证明最终Prompt自由文本全部正确或策略收益提升。
