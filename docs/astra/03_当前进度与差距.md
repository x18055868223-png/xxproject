# 当前进度与差距

核对日期：2026-09-07。本文的“当前”指本次可访问的代码与资料状态，不是本日实盘状态。版本/文件指纹见[台账](02_版本与证据台账.md)，理论裁定和反例见[知识库](knowledge/01_核心思路与尝试档案.md)。

## 1. 主路径应怎样理解

目前可证的主路径分成两条在研工作流，而不是一个已经自动闭环的系统：

1. **信号使用主线**：实时因子 → producer `1.5.7` 的事件/固定轮次只读记录 → 审计页及 LLM 旁路 → 人工理解和选择如何使用。后续模型信号调整以这条线的目标、样本和真实消费方式为准。
2. **执行工程主线**：人工给定方向与约束 → 市场候选、确认与成交 → 持仓/对冲/退出/结算。独立执行 v1.5.2 是成熟行为的本地后续；事实优先重构为另一条当前候选线。源码不能证明用户此刻在 FMZ 运行的是哪一个。

旧“信号包直接驱动执行”设计并未由当前 manual-gate 自动实现；人工从审计页转为执行参数的决策，当前也缺少一份已验证的端到端归因链。**整理不能替用户默默选择一条实盘策略，也不能把尚未连接的模块画成已闭环。**

次级知识库保存核心思想、失败尝试与迁移经验。进入主线的条件不是名字熟悉，而是能说明目标、输入、实际消费者、失效条件和可检验的增量价值。

## 2. 分层进度

| 模块 | 理论/代码可证事实 | 历史验证记录 | 当前缺口 |
| --- | --- | --- | --- |
| 结构与信号 | 根可见 producer `1.5.7`；后续 xxproject `2c72162` 同版本。默认 `read_only_demo=True`，固定轮次为北京时间 23:00 | 原始 v0.5.4 交接记录置信标定事故、修复与复观要求；后续有 producer/审计契约测试资产 | 本次未复跑策略测试；事件与固定卡需分开采样，置信刻度不能直接解释为获利概率 |
| 时段/耐久 | 市场先验与 AUDIT_ONLY 耐久信息已有文档/代码；不直接授权交易 | 三年 K 线代理报告记载 315,363 根 5m bars，仅支持市场先验 | 真实信号增量效果、概率校准与期权收益尚不能由该报告推出 |
| VRP | 独立模块 v1.1；整合 manual-gate 有 `apply_vrp_gate` 消费和缺上下文不可锁定行为 | 阶段场景模拟与多时点验证要求有记录 | 新事实优先执行主链未发现同等 IV/RV 净补偿硬门；不能把净 credit>0 视为已经承接 VRP |
| 对冲 | 初期 `04_对冲模块` 为 v0.3 干跑快照；后续独立执行与重构已改变风险输入/动作链 | 旧模拟与新执行测试记录必须分别读取 | 旧 EDB/GGR/KPF 持续性设计不能整体套在当前概率带/Boll 风险链；模型概率质量仍待校准 |
| GEX 数据 | gex 仓库 main 为 6 月滚动 rank API；xxproject 8 月分支已迁移 public JSON，并补健康诊断 | 后续提交与历史项目记录可证工程迁移 | 本日端点、数据年龄、覆盖范围、单位一致性未现场核验；可读数据不等于当前有效证据 |
| 审计前端 | manifest/单卡阅读；`f33c614` 后已有有限加载及发布修复 | 8/28 接手包记录 173 卡、全部详情 Promise.all 和 LLM ERROR | 该事故记录不能当 8/29 代码现状；本日线上页面仍需另行采样 |
| LLM 旁路 | `2c72162`：主 schema `1.5.1`、Prompt `1.5.6`、entry `1.1.10`；transition schema `1.3.0`、Prompt `1.3.2` | DeepSeek 迁移、有界恢复、综合辅助与发布检查均有代码/历史记录 | 严格校验及结构完整并不证明推理有新增预测价值；旁路不生成交易许可 |
| 人工审计执行 | 整合交付 `3.0.0-manual-gate`，不消费信号层输入；真实门默认关闭 | README 记载 206 passed、bundle check 等 | 属于旧整合入口；不代表本地其他执行项目门也关闭或当前已实盘验收 |
| 独立执行 | 远端 main 正式 v1；工作分支 `76e7371` 为 v1.5.1；本地 v1.5.2 含未提交修改 | v1.5.2 交接稿记载 661 passed 与匹配 artifact 哈希 | 真机命令、订单终态、残值收束与恢复路径仍需精确版本的外部证据 |
| 事实优先重构 | 当前本地 `0.7.11-g8.3.8 / G8_3_8_FMZ_PERSISTENCE_ROUNDTRIP_RC`；本地 git 无提交 | README 声明仅 LOCAL_STATIC_CHECK / LOCAL_TEST_PASS / LOCAL_BUNDLE_PASS | 正确性与可用性需一起验证；不能把本地候选或门开启视为已上线 |

上述版本来自本轮代码读取或明确标示的历史文件。远端入口：[后续整合树](https://github.com/x18055868223-png/xxproject/tree/2c7216229a0f64c95cc37355e6151bf0cee01189)、[独立执行 v1.5.1 分类](https://github.com/x18055868223-png/Neutral-Loop-Execution-Layer/blob/76e737144fb4e5cf228c8562de944624d1e21f37/artifacts/VERSION_CLASSIFICATION.md)。

## 3. 当前路径的校对裁定

### A. 方向与开仓权已主动分离，但用途链尚不完整

整合 `demo/execution_build_manual_gate/realsrc/src/strategy.py` 的人工审计主链和最新交付 README 都明确由人工参数计划，不是接收 producer 的事件后自动执行。这是有意边界调整，不是死代码证据。

校对结论：第一性“分工与拒绝条件明确”仍在；早期“系统自动连接”的整合叙事已不适合当前入口。后续研究应先记录某条信号究竟影响了人工的方向、等待、结构审视还是放弃，否则无法判断信号有没有实际价值。

### B. 定价补偿职责在新执行线存在待解释缺口

已核对的消费链：旧 manual-gate `strategy.py:299` 的计划预览及 `:1221` 附近运行分支调用 `apply_vrp_gate`；缺 `market_context` 标为 `VRP_CONTEXT_MISSING`，不生成可锁定候选。

新重构 `src/runtime_loop.py:839` 的 `_build_library` 调用 `build_candidate_library`；`src/planner.py:573–632` 计算 bid/ask credit、手续费、容量、最大损失和比值，`credit <= min_net_credit` 会拒绝。在这条主链及当前单文件中，检索并核对未发现旧 VRP 的 IV/RV 补偿输入或同等净 edge 门。

裁定是“**早期定价职责未在已查新路径中得到同等证明**”，不是“策略无效”或“必须立即回接旧 VRP”。净 credit 为正说明收入方向与基本结构可行，不足以证明承担的尾部风险得到足额补偿。是否改由人工承担、采用另一种定价模型，或保留为待实现职责，应成为单独的版本化决定。

### C. 有些概率名称实际是代理刻度，而且确实影响排序

新重构 `src/planner.py:619` 将 `win_rate` 定义为 `1-abs(short.delta)`，`:851` 附近把它与收益风险/资金效率放入综合排序。因此它**不是不被消费的形式字段**，而是被消费但名称容易过度承诺的代理。

同项目 `src/hedge_policy.py:45` 的 `first_touch_probability` 在 IV 可用时使用无漂移对数距离近似，返回 `HIGH`；缺 IV 时以 `1.8×abs(delta)` 有界回退并标 `LOW`。`position_manager.py:1276–1327` 用它形成当前概率、相对入场漂移并分类。`HIGH` 在这里反映输入/方法分支，不能当成统计置信区间或已校准概率。

后续应先改正研究与展示解读，再决定是否校准或替换模型。本次只在整理文档中校正概念，没有修改上述代码或现有 UI。

### D. 只读层需要验证“被怎样使用”，而不是要求它越权

时区、耐久与 LLM 旁路的 AUDIT_ONLY 设计有明确理由，不能因为不直接改变订单就判为形式主义。判断其实际价值应查：是否改变了使用者对证据有效期、反例或等待条件的理解，以及这些改变能否留下可复核记录。

反过来，如果只新增一个分数、一个段落或一个 “Bayesian” 标签，却没有目标事件、先验/似然依据、实际消费者或对照检验，也不能把它算作新增推理能力。当前 LLM 的结构化报告存在，不等于真实后验概率模型已经训练或验证；详见知识库的判定规则。

### E. 已找到默认权重关闭的实际案例

整合 manual-gate `config.py:39` 的 `PLAN_WEIGHTS.manual=0.0`。`plans.py` 中 confidence→preferred_delta→delta_fit 的计算仍存在，但初筛及综合排序都乘以该零权重，调用方也实际使用此配置。因此该契合项在这两个排序阶段不贡献分数，不能只因看到计算函数就写成“置信驱动选腿”。此结论仅限该默认配置和阶段，详见[知识卡 K12](knowledge/01_核心思路与尝试档案.md)。

现有 LLM Prompt 则已经明确把情景权重标为主观参考；不要把有意的旁路、关闭的排序项、被消费但未校准的概率代理归为同一种问题。

## 4. 版本与权限必须按项目分别展示

| 项目/资产 | 本轮直接读到的配置 | 不能据此推出 |
| --- | --- | --- |
| 整合 producer 1.5.7 | `read_only_demo=True` | LLM/人工认可后可以自动交易 |
| 整合 manual-gate 3.0.0 | entry/exit/hedge 为 False，`DRY_RUN_PASSED=False` | 所有独立执行项目也处于空跑 |
| 独立执行本地 v1.5.2 | `RUN_PROFILE=LIVE`；entry/exit/hedge 为 True，`DRY_RUN_PASSED=True` | 本日已经通过 FMZ 或实盘验证；常量是配置，不是验证事件 |
| 重构 0.7.11-g8.3.8 | `WRITE_GATES` 的 live/entry/exit/hedge/cancel/archive/dry_run_passed 为 True | 绕过确认、事实完整性、正 credit、持久化与交易所重读 |

在当前重构中，“保护交易所事实、阻断错误写入”与“防止合法动作被无关本地状态永久压住”同属主链原则。只能以实际订单/持仓语义检查，不能用“更保守”三个字替代正确性证明。

## 5. 历史验证与本轮验证

历史材料记载的 264、206、661 项测试分别属于旧 v2.9.9、整合 manual-gate 与独立 v1.5.2；本次没有运行这些交易测试。KPF/SLRP 历史实验也不改称本轮回测。

本轮实际进行的是：远端分支/tag/Release 核对、本地文件哈希、关键代码消费链检查、历史报告与结果文件的有限校对、文档链接与提交范围检查。另用 Black–Scholes 基础公式构造了 Delta 与到期概率不相等的数值反例；这是概念检查，不是策略回测或收益验证。

具体检查包括61份本地来源的 SHA256；主仓引用的36个提交对象；文档相对导航及固定 SHA 格式。对整合 manual-gate 只提取纯排序函数，验证默认 manual=0 时改变 delta_fit 不改变初筛与综合分，非零权重对照会改变初筛分；未导入或运行策略入口。KPF 的56/520样本与等级单调性对照原JSON；SLRP 核实验168条配对结果重算均值，当前链情景结果核对 summary；这些均为历史输出一致性检查，未重新取得行情或回测。

目前最需要补齐的不是更多形式字段，而是三条证据链：**信号如何影响使用决策、风险补偿如何得到判定、执行结果如何反哺模型**。主路径与知识库的后续试验应围绕这三条链展开。

## 6. 2026-09-07后续研究停点

**第一优先任务已具体化**：[记忆第0B节](PROJECT_MEMORY.md)建议做真实信号信息增量诊断。本轮窄查根目录前端manifest（5张卡）、最多3张单卡、样例JSONL及市场先验文档；抽查卡存在fixture痕迹，所指`demo/logs`本地不存在，样例归档仅1条SAMPLE，三年K线先验不是真实信号效力记录。当前所查范围尚缺真实快照来源闭合及后续价格配对；没有据此断言运行端无日志，尚未执行诊断或访问运行端。

**本轮核心决策重评**：完成[研究记忆第0A节 D01–D08](PROJECT_MEMORY.md)。再次核对根目录1.5.7：`info`按投票幅度缩放，confidence为带下限调节的内部合成刻度，veto会将最终lean设为NEUTRAL且另存门前方向。这证明有明确的语义/消费检查入口，不证明实际预测漂移、信息永久丢失或门控错误。优先级调整为关键合成语义诊断与简单信号/路径对照，单卡不再作为主要交付终点；尚无本轮市场样本或策略优劣结果。

**最新纠偏**：AI 应从事实分析、反驳和推荐，不能将未决问题转交用户定规则。用户提供多方案实现成本低这一条件；已撤回由既有价差资产和暂时收口导出的单一优先/对照数量限制。当前主问题为信号信息与决策增量，工具候选保持开放；尚未证明单一或多方案更优。[内容信任协议](knowledge/03_单次信号内容信任协议.md)已补分析职责与决策相关性，尚未对真实卡执行，也未开展跨方案比较。下列同日前序建议按历史保留，以[研究记忆第0节](PROJECT_MEMORY.md)为准。

用户提出是否固定价差或允许网格/突破等多方案。已形成主方案加不交易、一个研究对照的范围建议；尚未执行跨方案比较或证明现有价差最优。多方案同时持有需要组合风险验证，按环境切换还需要选择规则的独立增量验证；两者不能混为已解决的“如何交易”。具体边界见研究记忆第3节。

用户后续明确固定末日/周末轮交易与未知信号寿命之间的适配问题。已核对TTL及市场先验的时间语义，尚无本轮真实信号期限效力检验；“状态仍活跃”不能升级为“对交易期间有效”。彼时曾建议先做时间语义与可比窗口诊断，再检验单一接受/否决用途；该用途不再作为唯一优先入口，当前以[研究记忆第0A节](PROJECT_MEMORY.md)为准。

用户明确当前主要用途为末日轮垂直信用价差，并提出信号利用困难、置信评级不清和疑似迭代漂移；同时要求在Astra内持久化核心结论。已建立[项目研究记忆](PROJECT_MEMORY.md)。本轮直接复核用户可见根目录1.5.7的confidence合成与UNCALIBRATED状态，Astra隔离工作树仍保留较早代码底座；尚未执行同版本十案例诊断或前向交易用途试验，漂移与收益增量均未被证实。最小用途比较属于研究建议，未调整权重、交易门或生产策略。
