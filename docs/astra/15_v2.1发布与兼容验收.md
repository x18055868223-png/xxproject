# Astra v2.1 发布与兼容验收

用户于2026-09-10审阅本地8879页面后明确授权“推送并完成更新”。目标为 xxproject 的 codex/astra-signal-rating-v1，不合并 main。代码、数据保全与正式页面已完成本轮部署验收；自然新卡的2.1前向评审仍待后续验证。

## 版本与保留边界

- 输出、Prompt、展示投影为2.1.0；事实包2.0.0，FMZ1.6.0保持不变。
- 更新评级命题、证据角色、两侧比较以及价格/倾向阅读入口；不改变信号权重、触发、执行许可和模型配置。
- 原始审计、历史评审、尝试/响应、预算、排除清单及配置保留。旧卡按原协议读取；新投影不冒充新原生评审。
- 不把隔离研究结果写入生产，不批量重评历史卡，不恢复已取消的Codex跟进任务。

## 发布前证据

13个相关测试入口、打包和1440/1280/390浏览器检查通过；发布前重新核对7项核心及交付指纹全部一致。独立审查核对核心语义、前端/物化链路和部署保留边界。

两张隔离真实卡各一次HTTP，结果及token/耗时见方案14。最终Prompt补充了反例，但未再次调用API，因此不能把早前样本当成最终Prompt的前向验收。

## 已知解释局限

研究固定轮比较反转条件仍有因果校对项：上行增强靠近Call墙本身通常削弱Call适配，必须另有上冲受阻、回落或传导减弱等事实才可能支持相反结论。最终Prompt已补反例；本地校验不等于完整自然语言因果证明。研究卡不进入生产。生产前向验证以部署后自然生成的新卡为准，不为获得满意等级重复请求。

## 回退原则

切换前先暂停两个审计timer并等待oneshot结束，备份整套代码、阅读投影、配置和运行数据。回退同时恢复旧代码和对应旧阅读投影；新版记录、尝试及预算另存并保留，不能删除或重置额度。回退旧代码时先保持LLM自动运行暂停，确认它能安全识别新增状态后再恢复，避免旧代码再次请求新卡。

## 实际部署结果（2026-09-10）

- 服务器代码与最终前端提交：[xxproject / 5b8b38e](https://github.com/x18055868223-png/xxproject/commit/5b8b38e6736b7a0ec0c26e4379bb868232347d41)；主实现为 c2cb19b，随后窄修旧卡方向标签与旧失效条件标题。后续纯回执提交不改变部署代码。
- 正式入口：http://13.231.16.198/signal-audit/。服务器部署schema/Prompt为2.1.0，页面价格、独立倾向及双B行动文案已核验。
- 15张历史卡的原评审与来源完整性信息不变，15组列表/详情投影一致；配置、尝试、响应、预算、sidecar、排除清单共15个受保护文件未改变。当天HTTP用量保持3次，没有因升级重评历史。
- 服务端整套备份位于 `/opt/signal-audit-backups/astra-before-c2cb19b-20260910/`，包含旧代码、旧阅读投影、配置、运行数据及source快照，SHA256已校验。
- 部署前暂停审计timer、确认oneshot均退出，安装时保留整个signal_cards目录。完成检查后恢复原有两个审计timer；没有新增定时任务。
- 恢复后完整服务器自检为27通过、2提示、0失败；提示为只读检查说明及退出自动路径的旧独立变化复核。两轮正常runner均 attempted=0、written=0、errors=0，用量仍为3；验证升级和恢复没有重复请求历史卡。
- 公共HTTP资源与部署文件一致；浏览器确认正式列表/详情可读。上线实页发现两处历史展示遗漏，已窄修：旧方向枚举改中文；旧含混失效条件明确保留为旧版，不冒充新版削弱适配条件。
- 最终浏览器检查1280及390宽度，12个展开区均可读，无横向溢出、断链或主阅读区机器字段泄漏，控制台无错误。主agent复核worker窄修与验收，explorer核验资产指纹/FMZ版本，reviewer独立核对发布保留边界及最终5b8b38e补丁。
- 兼容自检针对实际已有2.0.0/Prompt2.0.1卡显式核对；安装的2.1.0协议另按代码、文件和模块导入检查。旧独立transition LLM存在历史告警，该路径已退出新卡自动评审，本轮不重跑它。
- 最新自然卡仍是9月10日05:39事件卡，FMZ1.6.0，原LLM2.0.0、Put B/Call B、偏空；不能将研究C/B写回它。当前没有部署后自然2.1卡，不宣称新的生产评审已经前向通过。
- FMZ交付物未修改：工作树与用户可见根目录SHA256均为 `71734726d2922e3cff7d0d49840ef77b1b347b0721b3416d8ee5cbbc94e3f2b1`，本轮无需替换FMZ实例。

## 本地交接与回退入口

当前代码工作树为 `.worktrees/astra-signal-rating-v1/`；根目录只同步本轮Astra说明，保留原有源码修改。不得把根目录旧app.js当作本次发布源码。下一轮从该完整工作树与xxproject工作分支开始。

部署脚本、包与验收日志保存在用户可见目录 `.artifacts/astra-v21-deploy-20260910/`。`rollback.sh`为显式人工回退入口，执行前先校验备份。脚本先保存升级后的完整数据，再成套恢复旧代码和阅读投影；保留当前尝试和预算，保持timer暂停等待旧代码兼容确认。本轮仅检查回退脚本语法和路径，未在生产执行回退演练。

最终验收摘要：`{"protected_files_unchanged": 15, "original_reviews_unchanged": 15, "projection_pairs_checked": 15, "installed_assets_equal_package": 13, "deployed_schema": "signal_llm_review@2.1.0", "deployed_prompt": "signal_llm_review_prompt@2.1.0", "latest_card_id": "20260910T053958+0800-BTC-nr_1788988008200_UP-acc4", "latest_fmz": "1.6.0", "latest_llm_schema": "signal_llm_review@2.0.0", "budget": 3, "new_natural_v21_card": false}`。
