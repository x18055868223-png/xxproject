# 执行层重构 v3 — 进度与变更说明

> 配套设计：`docs/当前执行层垂直价差落地设计.md`（v2.0 主体框架）+ 计划 v3.1 修订（基于 `docs/补充意见.txt`，已逐条核对源文件）
> 唯一基座：`demo/execution_build/realsrc/src/`；验收后 `build_bundle.py` 重生成单文件覆盖 `demo/最新交付物/spm_calendar_protected_short_v1.py`
> 安全：默认全空跑（5 门控全 False）；`GetCommand` 在 FMZ 回测不生效，**须真实机器人空跑验收**
> 测试基线：`python demo/execution_build/realsrc/tests/run_all.py`

---

## v3.1 对《补充意见》的处置（摘要）

- **事实校正**（已核对源文件）：信号层最新交付物实为 **demo v1.1.0 / schema `nrd.schema.v1.0.0`**；`allow_downstream_evaluation` **已存在**（`neutral_regulation_demo_fmz.py:7063`）。补充意见的「v0.5.4 / 字段缺失」系旧快照口径。
- **采纳的工程结论**（全部纳入实现）：
  - P0-1 端到端接收链确实缺失 → 执行侧 `signal_receiver` + 降级（总线不可用禁新开、不阻断持仓管理）。
  - P0-2 冻结审批快照取代「实时质量哈希=确认码」；一次性确认码 + 结构化幂等键 + 漂移容差复核。
  - P0-3 拆分单一 `ALLOW_TRADING` 为 5 门控。
  - P0-4 动作仲裁四输出（preferred/executable/blocked/fallback），退出不可执行时回退对冲。
  - P0-5 对冲 open/reduce 分离（reduce_only 不能建仓）。
  - P0-6 投影预算去 PLACEHOLDER，真实算法 + fail-closed。
  - P1 风险退出授权补全语义；面板「胜率」标注 `≈1−|Δ|`；账本数=9；显式 `SHORT_FLAT_LONG_RESIDUAL`；真实机器人空跑验收。

---

## 进度日志

### E0 基座清理 — 完成（2026-06-05）
彻底删除日历价差运行路径：
- `config.py`：删 `ENABLE_CALENDAR / PROTECTION_RESIDUAL_RECOVERY / PROT_DTE_DAYS` 及其全部校验（含修复对已删 `PROTECTION_RESIDUAL_RECOVERY` 的悬挂引用——测试不跑 `main()` 故此前未暴露的潜伏 NameError）。
- `plans.py`：删 `MODE_CALENDAR / plan_covered_cycles / 日历分支`；`plan_effective_credit(short,prot)`、`plan_assemble(...)` 简化为垂直唯一（保留 `mode/covered_cycles/residual_value` 键为垂直平凡值，下游零改动）。
- `strategy.py`：删 `cal_exps` 枚举与日历 specs 循环；垂直保护腿取同到期 `s_insts`。
- `accounting.py`：`structure_type → VERTICAL_CREDIT_SPREAD`。
- 测试：3 文件改垂直唯一断言（新增「短腿与保护腿同到期」硬约束）。
- KPF 已于前序 Phase1 移除（确认无残留）。
- **验证**：`run_all.py` → **80 passed, 0 failed**；全 src `py_compile` 通过；`validate_config()` → `[]`。
- 残留（低优先，shadow-only）：`gex_info` 增强尚未并入 `realsrc/src`，挪到 E8 前清理 sweep。

### E1 主链骨架 + 交互控制台 — 进行中

#### E1.1 执行授权门控拆分 — 完成（2026-06-05，补充意见 P0-3）
- **新增 `gates.py`**：纯函数 `gate_decision(action, allow_entry, allow_exit, allow_hedge, kill_new_risk, emergency_reduce_only)` + `gate_summary`；动作 `ENTRY / EXIT / HEDGE_OPEN / HEDGE_REDUCE`。
- **`config.py`**：新增 5 门 `ALLOW_ENTRY_TRADING / ALLOW_EXIT_TRADING / ALLOW_HEDGE_TRADING / KILL_NEW_RISK / EMERGENCY_REDUCE_ONLY`（默认全 False）+ 布尔校验；`ALLOW_TRADING/KILL_SWITCH` 标 DEPRECATED 保留待 E8 移除。
- **`execution.py`**：进场 `live` 改由 `gate_decision(ENTRY)` 判定。
- **`build_bundle.py`**：`MODULE_ORDER` 注册 `gates`。
- **关键语义**：EXIT 不被 kill/emergency 阻断（降风险）；HEDGE_OPEN 仅被 emergency 阻断（kill 下仍可开对冲压尾部）；HEDGE_REDUCE 恒 `reduce_only` 且 kill/emergency 下仍允许（风险收口）。
- **修复测试副作用**：`test_execution.py` 旧 live 用例靠 `ALLOW_TRADING=True` 启真 → 迁移到 `ALLOW_ENTRY_TRADING`，避免断言失败前未还原 monkeypatch 而级联污染后续 flow 测试。
- **验证**：新增 `test_gates.py`（6 例）；`run_all.py` → **86 passed, 0 failed**。

#### E1.2 命令路由 + 命令账本 + 幂等 — 完成（2026-06-05，补充意见 P0-2）
- **新增 `cmd_router.py`**：`parse_command`（中英按钮名→规范类型 `EXECUTE/REJECT/EXIT_AUTHORIZE/EXIT_REVOKE/RISK_EXIT_AUTHORIZE/KILL/RESUME`）、`idempotency_key(robot_id|session_id|refresh_seq|command_type|nonce)`、命令账本 `cmd_ledger_*`（`_G` 持久化、上限 200 条）、`route_command`。
- **幂等语义**：消费型命令（执行 / 止盈授权 / 风险退出授权）以 `arg`（确认码 / 授权码）作 nonce，**一次性消费**——同键已在历史账本 → `DUPLICATE` 忽略（防跨轮 / 重启重复下单、防延迟旧命令命中新方案）；`refresh_seq` 变（库刷新）→ 新键 → 同码复活放行；切换型命令（拒绝 / 撤销 / 急停 / 恢复）不去重但全部入账本审计。
- **`config.py`**：新增 `ROBOT_ID`。**`build_bundle.py`**：`MODULE_ORDER` 注册 `cmd_router`。
- **验证**：新增 `test_cmd_router.py`（8 例）；`run_all.py` → **94 passed, 0 failed**。

#### E1.3 信号→执行接收链 + 降级 — 完成（2026-06-05，补充意见 P0-1）
- **新增 `signal_receiver.py`**：`validate_signal_package`（纯函数，校验 `schema_name` / 版本前缀 / TTL `expires_ts` / `reject_state` / `data_quality` / 可执行 `side_hint`）、`receive_signal`（按源 OFFLINE_MANUAL / FILE / G 接收）、`load_package_from_file|g`、`signal_lineage_*`（package_id 血缘记账）。
- **裁决语义**：OK → tradeable；MISSING / STALE / REJECTED / BAD_QUALITY / SCHEMA_MISMATCH / NO_SIDE → `block_new_opens=True`（**只**禁进场，不阻断持仓管理）；OFFLINE_MANUAL → tradeable=None（静态 `SIGNAL_STATE` 决定，面板须红标）。
- **`config.py`**：新增 `SIGNAL_SOURCE / SIGNAL_FILE_PATH / SIGNAL_G_KEY / SIGNAL_SCHEMA_VERSION_PREFIX` + 校验。**`build_bundle.py`**：注册 `signal_receiver`。
- 依赖（follow-up）：信号侧调 `signal_bridge` 落盘 + 原子 rename + 同托管 loopback spike。
- **验证**：新增 `test_signal_receiver.py`（10 例）；`run_all.py` → **104 passed, 0 failed**。

#### E1.4 交互控制台 + 操作提示 — 完成（2026-06-05）
- **`display.py` 新增**：`disp_console_table`（状态栏顶部「交互页面」：阶段 / 执行门控 / 信号接收 / 待批方案+确认码 / 软授权 / 退出活动 / 操作提示）、`disp_operation_hint`（阶段→人话操作提示引擎，急停优先）、`disp_gate_line`、`disp_signal_status_line`、`_console_phase_cn`。
- `disp_status_panel` 表数组**置顶**注入交互控制台；后续阶段（E2 确认码 / E5 软授权 / E6 退出进度）经 ctx 字段填充对应行。
- **验证**：`test_display.py` 更新（4 表 + 控制台置顶）+ 新增 3 控制台用例；`run_all.py` → **107 passed, 0 failed**。

### E1 小结（2026-06-05）
四项交互/安全基础——**门控拆分(gates) / 命令路由幂等(cmd_router) / 信号接收降级(signal_receiver) / 交互控制台(console)**——完成，全程纯函数 + `_G` 持久化，**107 passed, 0 failed**。
原 E1.5「PLAN/ORDER 双脚本→单一持续主链」与 E2「推荐库 + 硬授权」强耦合（主循环的目的即维护推荐库并处理硬授权命令），故**合并入 E2** 一并落地，避免半成品主链。

### E2 推荐库 + 硬授权（短确认码）+ 单一主链 — 进行中
单一持续 `run_cycle` 主链取代 PLAN/ORDER 双脚本：每轮 轮询命令(cmd_router) → 信号接收(signal_receiver) → 门控(gates) → 维护垂直推荐库 + `strategy_code/quality_code/plan_hash/confirm_code` → 冻结 `approval_snapshot` → `执行:码`/`拒绝` 硬授权（漂移容差预提交复核）→ 锁定 + 预提交 → 渲染控制台。
- 计划子步：E2.1 推荐库编码 + 短确认码 + 冻结审批快照（纯函数，先行可测）；E2.2 `run_cycle` 主链 + 命令分发 + 控制台填充；E2.3 main() 切换 + 既有 PLAN/ORDER 测试迁移。

#### E2.1 推荐库编码 + 短确认码 + 冻结审批快照 — 完成（2026-06-05，补充意见 P0-2 核心）
- **新增 `recommend.py`**：`strategy_code`（`VCS|side|expiry|short|long` 稳定结构身份）、`quality_code`（冻结质量口径哈希：signal 包 / S:PM 释放 **0.1 分桶** / VRP / 预算决定；**不含实时行情、不含 refresh_seq**）、`plan_hash`、`confirm_code`（Base32 前 4，库内冲突 `ensure_unique_confirm_codes` 自动延长）、`build_approval_snapshot` / `build_recommendation_library`、`resolve_confirm_code`（大小写不敏感、仅 QUALIFIED）、`precommit_recheck`（漂移容差：`plan_hash` 一致 + live 硬门，不要求实时哈希全等）。
- **P0-2 已由测试证明**：确认码在子桶波动（0.83→0.85，同 0.8 桶）下**稳定不变**（用户输入期间不翻动）；材料级变化（跨桶 0.83→0.72 / 换信号包 / 结构变）→ 码变 → 旧码在新库 `resolve_confirm_code` 返回 `None`（自动过期）。`refresh_seq` 单列于快照供 cmd_router 幂等键，不污染确认码稳定性。
- **`build_bundle.py`**：注册 `recommend`。
- **验证**：新增 `test_recommend.py`（6 例）；`run_all.py` → **113 passed, 0 failed**。

#### E2.2 单一持续主链 run_cycle — 完成（2026-06-05）
- **`strategy.py` 新增 `run_cycle(now_ms)`**：每轮 轮询命令(`GetCommand`→`route_command`，全部入命令账本、消费型严格幂等) → 信号接收(`receive_signal`) → 门控汇总(`gate_summary`，含运行时【急停】`_G` 覆盖) → 无持仓且信号放行时维护垂直推荐库(节流 `PLAN_REFRESH_SECONDS`) + 短确认码 → 填充交互控制台 → `_emit`。
- **命令分发**：`执行:码`→`resolve_confirm_code`→锁定不可变副本 + 预提交(`precommit_recheck`)；`拒绝`清锁回待批；`急停`置运行时 kill(进场✗、退出/对冲减仓不受影响)；`恢复`清 kill + 清锁(要求新硬批准)；`授权止盈/撤销/风险退出授权` 路由占位(E5)。
- **验证**：新增 `test_run_cycle.py`（6 例：建库→硬授权锁定/预提交、重复执行幂等忽略、错码/旧码拒绝、急停覆盖进场门+恢复、拒绝清锁、离线阻断信号不建库）；`run_all.py` → **119 passed, 0 failed**。

#### E2.3 main() 切换单一主链 — 完成（2026-06-05）
- `main()` 弃 PLAN/ORDER `ROUND_MODE` 分支，改为 `while: run_cycle()`；启动 `ledger_reconcile`（E4 强化恢复阻塞）。`_plan_round/_run_order/_build_menu` 暂留作测试/复用（E8 清理）。
- **bundle smoke 通过**：`build_bundle.py --check` 合成单文件(3849 行)、语法编译 + 名称解析 smoke 全过、无 KPF 残留——5 个新模块在扁平命名空间内全部解析。

### E2 小结（2026-06-05）
推荐库 + 短确认码硬授权 + **单一持续主链**落地，旧 PLAN/ORDER 不再是运行入口。**119 passed, 0 failed** + bundle 合成通过。

### E3 受控真实开仓 + 入场快照冻结 — 完成（2026-06-05）
- **E3.1 投影预算真实算法（P0-6）**：`risk_controls.evaluate_projected_budget(proposed,current,limits)`——把**拟建仓位**(gamma/vega/结构保证金/最大价差损/对冲保证金预留/费用预留)计入当前组合后与 `PORTFOLIO_LIMITS` 比较；任一必填缺失 → fail-closed(BLOCK)。`config.PORTFOLIO_LIMITS` 新增(占位阈值)。`test_projected_budget.py`(6 例，含「current 0.49<上限但拟建后 0.508>上限→BLOCK」证明非 PLACEHOLDER)。
- **E3.2 预提交 13 项硬门**：`recommend.evaluate_precommit_checks(locked,lib,live)`——signal_fresh / same_signal_package / locked_plan_hash_match / locked_quality_code_match / vertical_only / vrp_rechecked / spm_rechecked / quotes_rechecked / entry_net_credit_after_costs_positive / projected_budget_passed / ledger_reconciled / no_unknown_orders / spread_ok；缺数据即该项 False(fail-closed)。`test_precommit.py`(7 例)。
- **E3.3 入场快照冻结**：新增 `position.py`：`build_vertical_entry_snapshot` 冻结 `entry_profit_ceiling_net = 卖方实收−保护实付−入场费`(80% 阈值审计基准)，`target_profit=ceiling×0.80`、`max_total_exit_spend=ceiling×0.20`；`freeze_entry_ceiling` 守卫入场后不可覆盖。`test_position.py`(4 例)。
- **E3.4 主链接入受控真实开仓**：`strategy._attempt_commit`——locked → `_build_precommit_live` → 13 项预提交 → 受 ENTRY 门控真实 `exec_open_structure`(保护腿先成交) → 冻结入场快照 → `_POSITION_KEY` 持久化 + 状态 `SHORT_ACTIVE_PROTECTED`；预提交不过/门关 → 仅空跑预览(order_intent + 预提交分解)；`commit_attempted` 防重复真实下单。控制台新增「预提交/开仓」行。`test_run_cycle.py` +2 例。
- **OFFLINE 默认 fail-closed**：无 market_context → `vrp_rechecked=False` → 默认不真实成交、仅空跑预览(VRP 双门待总线模式接入)。
- **验证**：`run_all.py` → **138 passed, 0 failed**；bundle 合成(4148 行) + smoke 通过。

### E4 管理主循环 + 启动恢复 + 四输出仲裁 — 完成（2026-06-05）
- **E4.1 四输出仲裁（P0-4）**：`risk_controls.unified_action_arbiter` → preferred / executable / blocked_reason / fallback。优先级 RECOVERY_BLOCKED>ORPHAN_HEDGE_EMERGENCY>MANAGE_IN_FLIGHT>EXIT_PREFERRED>HEDGE_READY>TAKE_PROFIT_READY>HOLD；退出未授权/无数据/预算暂停时 executable **回退对冲**(若可执行)，不压住风险收口。`test_arbiter.py`(8 例)。
- **E4.2 启动恢复**：`ledger.evaluate_startup_recovery`(纯函数)——身份不明订单 / 账本短腿但交易所无期权 → RECOVERY_BLOCKED；有 BTC-PERP 对冲但无期权卖方风险 → ORPHAN_HEDGE_EMERGENCY；恢复完成前禁开新仓。`test_recovery.py`(6 例)。
- **E4.3 管理主循环接入**：`strategy.manage_cycle`(10 域骨架：对账 + 四输出仲裁)；`startup_recovery_check` 读真实持仓→裁决落 `_G`，`main()` 启动调用；`run_cycle`：恢复阻塞→RECOVERY_BLOCKED 禁开、有持仓→`manage_cycle`(急停下仍运行)；控制台新增「风险动作」行。`test_run_cycle.py` +2 例。
- **验证**：`run_all.py` → **154 passed, 0 failed**；bundle smoke 通过。

### E5 软授权（持仓退出授权）— 完成（2026-06-05）
- 新增 `authorization.py`：`PositionExitAuthorization` 合同(UNAUTHORIZED/AUTHORIZED/REVOKED/CONSUMED) + `auth_code`(持仓授权短码) + `build_authorization` / `is_authorized`(与 position_id 绑定、valid_until 过期) / `revoke` / `consume` / `authorize_from_code`；风险退出授权补全 P1 语义(max_exit_spend / allowed_order_types / valid_until)。`test_authorization.py`(6 例)。
- 主链接入：`授权止盈:码` / `风险退出授权:码` → `authorize_from_code`(校验码匹配当前 position+policy) → 落 `_EXIT_AUTH_KEY`；`撤销授权` → revoke；与 position_id 绑定、并行非阻塞、`_G` 持久化(可重启恢复)。`manage_cycle` 用 `is_authorized` 喂入仲裁 `exit_authorized`；控制台「软授权」行显示状态 + 未授权时显示授权码。`config.RISK_EXIT_MAX_SPEND` 新增。`test_run_cycle.py` +2 例。
- **验证**：`run_all.py` → **162 passed, 0 failed**。

### E6 止盈资格 + 低成本退出 + 保护腿回收 — 完成（2026-06-05）
- **E6.1/E6.2 纯核（position.py）**：`reference_profit_capture_ratio`(保护腿价值**不入分母**、数据缺口→None)、`take_profit_qualified`(≥80%)、`short_buyback_budget`、`short_buyback_price_cap`(由剩余预算反推、floor 到 tick)、`within_exit_budget`、`exit_campaign_decision`(IDLE/WAIT_TRIGGER/WORKING_SHORT/PAUSED_BY_BUDGET/PAUSED_BY_DATA/WORKING_LONG)、`protection_recovery_decision`(无 bid→LONG_RESIDUAL_ONLY)。`test_takeprofit.py`(7 例)。
- **E6.3 退出执行器（execution.py）**：`exec_exit_buyback_step`——≤price_cap 被动 post-only 买回短腿、撤单后再查捕捉晚到成交；`allow_live=False` 仅意图。config 增 `EXIT_*`。
- **E6.4 主链接入（manage_cycle）**：`_evaluate_take_profit` + `exit_campaign_decision` → 受 `ALLOW_EXIT_TRADING` 门控真实买回(默认空跑) + `_apply_exit_fill`(减剩余短腿/加已用支出，归零→`SHORT_FLAT_LONG_RESIDUAL`)；喂入仲裁 take_profit_ready/exit_executable/exit_pause_reason；控制台「止盈资格/退出活动」行。`test_run_cycle.py` +2 例。
- **验证**：`run_all.py` → **171 passed, 0 failed**；bundle smoke 通过。

### E7 BTC-PERPETUAL 对冲生命周期 — 完成（2026-06-05，补充意见 P0-5）
- `hedge_risk.py`：`adapter_preference=UNBOUND` → 固定 `hedge_instrument=BTC-PERPETUAL / venue=DERIBIT`。
- 新增 `hedge.py`(纯)：`hedge_side`(CALL→buy/PUT→sell)、`hedge_target_contracts`(短腿归零 / CLOSED / SETTLED→0；delta→USD 合约取整 min_trade)、`hedge_order_action`(**P0-5：OPEN/INCREASE 非 reduce_only；REDUCE/UNWIND 强制 reduce_only**)、`hedge_orphan`、`settlement_guard`。`test_hedge.py`(6 例)。
- `execution.exec_hedge_step`(gated；reduce_only 透传)；`deribit_io.dbt_place_order` 增 `reduce_only` 参数。
- `manage_cycle._evaluate_hedge`(perp 持仓 + instrument metadata + 目标/动作/孤儿) → 受 `ALLOW_HEDGE_TRADING` 门控下单(默认 dry) → 孤儿喂入仲裁 `orphan_hedge`；控制台「对冲」行。config 增 `HEDGE_REDUCTION_RATIO` 等。`test_run_cycle.py` +1 例。
- **验证**：`run_all.py` → **178 passed, 0 failed**；bundle smoke 通过。

### E8 生成覆盖最新交付物 + v3.0 文档 — 完成（2026-06-17）
- 版本升 `STRATEGY_VERSION 2.0.0`；`build_bundle.py` 头改「垂直信用价差」、`check()` 增「无日历残留」扫描 + v3 主链/交互/退出/对冲函数齐全断言。
- 重生成单文件(4778 行) → 覆盖 `demo/最新交付物/spm_calendar_protected_short_v1.py`（旧 v1.6.2 已存入 `副本快照/2026-06-17_执行v2.0.0_v3重构垂直唯一/spm_OLD_*`）；py_compile 通过。
- 更新最新交付物 README（执行层 v2.0.0 行 + 178 测试 + FMZ 按钮/真实机器人空跑验收说明）。
- 写最终设计 `docs/当前执行层垂直价差落地设计_v3.0.md` + `副本快照/.../快照说明.md`。
- **验证**：`run_all.py` → **178 passed, 0 failed**；bundle smoke（无 KPF / 无日历）通过。

---

## 总结（v3 重构完成 2026-06-17）
E0–E8 全部完成并验证。测试 **80 → 178 passed, 0 failed**，零回归。新增纯函数模块：`gates / cmd_router / signal_receiver / recommend / position / authorization / hedge`（+ 交互控制台、投影预算真实算法、四输出仲裁、启动恢复）。《补充意见》**P0-1~P0-6 + P1 全部落地**。默认 5 门控全空跑；FMZ 交互须**真实机器人空跑验收**（GetCommand 回测不生效）。**非"策略已验证"**——标定/实盘复观仍待。
残留（低优先，shadow-only）：`gex_info` 增强并入 realsrc。

---

## F1 对冲场所可选（Binance USDC maker-0）— 完成（2026-06-17，STRATEGY_VERSION 2.1.0）
- `hedge.py`：`hedge_venue_config`(DERIBIT 反向/BINANCE 线性 maker-0) + `hedge_target_contracts` 增 `linear` 换算。
- 新增 `binance_io.py`（`bnc_get_position_btc`/`bnc_place_hedge`，经 FMZ `exchanges[idx]`，maker post-only + reduce_only 平仓方向）；`fmz_shim` 增 `exchanges` 多所桩。
- `execution.exec_hedge_step` 改场所感知（venue_cfg 路由 Deribit/Binance）；`manage_cycle._evaluate_hedge` 按 `HEDGE_VENUE` 选场所/线性/perp 读取；控制台「对冲」行带场所标签。config 增 `HEDGE_VENUE` 等 5 项 + 校验。
- **显式配置选择、非运行时自动切换**（避免补充意见所警示 UNBOUND）；默认 Deribit、空跑。跨所对账/恢复取舍见说明文档 §6/§13。
- **验证**：新增 `test_hedge`(场所/线性) +2、`test_binance_io` +4、`test_run_cycle` Binance +1；`run_all.py` → **185 passed, 0 failed**；bundle smoke 通过。重生成覆盖交付物(v2.1.0) + 快照。

## F2 执行层全量代码审计 + 完整说明文档 — 完成（2026-06-17）
- 审计结论：v3 运行路径(`main→run_cycle`)自洽于新模块；**旧整合层(PLAN/ORDER、ExecutionSession、apply_vrp_gate、evaluate_position_risk、watch_position、portfolio_budget 占位)不在 v3 路径**（仅测试/bundle-smoke 引用，建议 E8.1 清理）；列明 7 项已知简化/缺口（真实开仓恢复、风险严重度未驱动仲裁、VRP 总线、对冲方向符号、币安下单形态、gex_info、标定）。
- 完整说明文档：`docs/执行层完整说明_v2.1.md`（概述/模块/主链/交互/门控/对冲场所/账本/状态机/配置/不变量/测试矩阵/部署验收/**审计发现**/后续优先级）。

## G1 开仓活动 entry_campaign（跨轮持久 maker + 信用底线）— 完成（2026-06-17，STRATEGY_VERSION 2.2.0）
- 起因：旧开仓为一次性追价(`commit_attempted`)，稀疏盘口成功率受限（见对开仓逻辑的判断）。
- `position.py`：`entry_net_credit` / `entry_credit_capped_index`(信用底线内最激进档) / `entry_campaign_decision`(IDLE/WORKING/PAUSED_DATA/PAUSED_CREDIT/ABANDONED/COMPLETE)。`test_entry.py`(3 例)。
- `execution.py`：`exec_entry_campaign_step`(保护腿先成交、逐 tick 向触价改善至信用上限、跨轮持久、部分成交记账) + `_post_maker_once`。
- `strategy._attempt_commit` **重写为持久活动**（进度 `prog` 存 `locked.entry`：prot_done/short_done/attempts/avg）：两腿达标→冻结入场快照；超 `ENTRY_MAX_ATTEMPTS`→放弃并回退保护腿残量。config 增 `ENTRY_MIN_NET_CREDIT/ENTRY_MAX_TICK_STEPS/ENTRY_MAX_ATTEMPTS`。控制台「开仓活动」行。
- 审计：**F2-C.1（真实开仓恢复）大幅闭环**——低成本(maker+信用底线、永不越价)与成功率(跨轮持久重挂、逐 tick 向触价改善)由参数权衡；残留小项见说明文档 §13.C.1（放弃回退失败的残值态、重启在途活动按交易所成交重校验）。
- 验证：`test_run_cycle` 增持久性用例（dry 跨 6 轮不一次性放弃、锁不清）；`run_all.py` → **189 passed, 0 failed**；bundle smoke 通过。重生成覆盖交付物(v2.2.0) + 快照。

## H1 持仓后链路补强（链路审计 P0①②③）— 完成（2026-06-17，STRATEGY_VERSION 2.3.0）
- 起因：整体链路审计发现持仓后三处**设计级缺口**——①双重持仓记录脱节（v3 用 `_POSITION_KEY`，reconcile/recovery 读 legacy led 失明）；②保护腿回收「决而不行」+ 会话永不 CLOSED；③仲裁器装饰化（退出/对冲各自独立执行、未单动作收口、退出期未禁新增对冲）。
- **P0①**：snapshot 增 `long_remaining_qty`；`position.position_reconcile`(快照 vs 交易所)；`evaluate_startup_recovery` 反向校验(`EXCHANGE_OPTION_BUT_NO_RECORDED_POSITION`，防重启重复开仓)；`startup_recovery_check` 改读 `_POSITION_KEY`；manage 每轮对账 surfaced(不阻断风险收口)。
- **P0②**：`execution.exec_protection_recovery_step`(无 bid→LONG_RESIDUAL_ONLY) + `strategy._archive_closed`；manage 短腿归零后回收保护腿、两腿 + perp 归零→`CLOSED`。
- **P0③**：manage 由 `unified_action_arbiter.executable_action` **单动作收口**；**退出活动期禁新增对冲敞口**(只许 reduce)。副带修复 P1「对冲常开」(对冲不再常开、仅 arbiter 收口的 fallback/orphan)。
- 仍开放(hook)：`exit_preferred/hedge_ready` 风险严重度未接 `hedge_risk`（见说明文档 §13 与 §14）。
- 验证：+`test_position`(reconcile)、+`test_recovery`(反向校验)、+`test_run_cycle`(CLOSED / 保护腿回收 / 退出期禁对冲 / 对账 surfaced)；`run_all.py` → **196 passed, 0 failed**；bundle smoke 通过。重生成覆盖交付物(v2.3.0) + 快照。

## R1 风险严重度→仲裁（接回 hedge_risk；副带对冲方向符号）— 完成（2026-06-18，STRATEGY_VERSION 2.4.0）
- 起因：审计 C.2——`unified_action_arbiter` 的 `exit_preferred/hedge_ready` 在 manage 中硬编码 False（风险严重度 hook），风险触发的主动退出/对冲从不发生；仅 take_profit 资格 + orphan 在驱动。
- **入场冻锚**：`recommend.build_approval_snapshot` 透传 `short_expiry/breakeven`；`position.build_vertical_entry_snapshot` 增 `entry_risk_anchor/short_expiry_ts`；`strategy._attempt_commit` 冻结快照时 `_build_entry_risk_anchor`(短腿 greeks + 入场行情 + breakeven loss_boundary)。新增 `_side_to_direction_bias`/`_dte_hours_to` 纯函数。
- **manage 风险裁决**：`strategy._evaluate_position_risk_now`(无快照/无锚→None) 调 `hedge_risk.evaluate_position_risk` → `tail_risk_state`；`EXIT_PREFERRED→exit_preferred`、`HEDGE_READY→hedge_ready` 替换两个 False hook。退出活动触发改 `exit_trigger = take_profit 资格 ∨ exit_preferred`（风险退出复用退出活动预算/价格上限/盘口/授权；需 `授权风险退出` POLICY_RISK_EXIT）。保持 P0③ 单动作收口 + 退出期禁新增对冲。`evaluate_position_risk` + 状态常量接回 v3 链（**E8.1 勿删**）。
- **对冲数量 + 方向符号（C.4）**：`hedge.structure_net_delta`(短腿−保护腿，缺保护腿退化短腿) 替代仅短腿 delta；`hedge.hedge_direction_consistent` 方向核对——反向时 `_evaluate_hedge` 禁 HEDGE_OPEN/INCREASE（仍许 reduce/unwind）。`_evaluate_hedge` 只在有 long_instrument 时查保护腿报价（避免 mock 默认报价污染）。
- 残留：OFFLINE 无总线 edb/ggr → persistence 恒 LOW → **HEDGE_READY 暂不可达**（仅 EXIT_PREFERRED 活跃，待总线 edb/ggr）；`RISK_EXIT_MAX_SPEND` 占位未标定；existing_hedge→HEDGE_ACTIVE 监控态。
- 验证：+`test_hedge`(结构净 delta / 方向核对) ×2、+`test_run_cycle`(入场冻锚 / 风险严重偏退出未授权回落 / 风险温和 HOLD / 风险退出授权后执行 / HEDGE_READY 执行) ×5；`run_all.py` → **203 passed, 0 failed**；`build_bundle.py --check` 通过。重生成覆盖交付物(v2.4.0) + 快照。

## R2 v2.4.0 全量审计整改（F1-F3 / C1-C3）— 完成（2026-06-18，STRATEGY_VERSION 2.5.0）
- 起因：对 v2.4.0 风险链路全量审计——F1 风险退出"能裁决不能成交"（被动 post-only + 错用止盈预算 + 越价不可成交时仍判可执行 → 卡死不回退对冲）；F2 风险退出在面板隐身（无 risk 行/无风险退出码/提示只引导止盈授权）；F3 缺盘口时风险静默判 NORMAL；C1 对冲下单 fire-and-forget（即查多为 0、price 可能 None）；C2 孤儿对冲清理被 `allow_hedge` 挡；C3 `no_unknown_orders` 桩恒 True + 重启在途活动不重校验。
- **F1**：`strategy._risk_exit_budget_cap`(用授权 max_exit_spend 反推、独立止盈缓冲、判 within=ask≤cap)；`execution.exec_exit_buyback_step(allow_taker)` 风险退出限价=cap 可越价吃单(成本硬封 cap·qty)；manage `exit_executable` 增 within、`risk_exit_unsatisfiable` 放行对冲回退。
- **F2**：`display.disp_risk_line` + 控制台「风险」行；`disp_operation_hint` 风险退出分支；run_cycle `exit_auth_state` 在 EXIT_PREFERRED 显示风险退出码；manage 返回 `risk_exit_auth_code/risk(pkg)`。
- **F3**：`_evaluate_position_risk_now` 短腿缺 delta∧缺 IV → `market_data_gap`(risk_state=None)，面板标注、不驱动动作。
- **C1**：`exec_hedge_step` Deribit None 盘口守门 + `Sleep` 等待 + 撤残单 + 二次查成交回报 filled；manage 捕获 `hedge_step` 入「对冲」行。
- **C2**：manage `orphan_cleanup`(裸 perp reduce_only) 绕过 `allow_hedge` 门（perp 已存在=场所已配置）；非孤儿对冲仍受门控。
- **C3**：`deribit_io.dbt_get_open_orders`(失败返回 None)；`_no_unknown_orders`(我方合约非我方 label 挂单/查询失败→False) 接入 `no_unknown_orders`；`startup_recovery_check` 无快照时用在途活动 prog + 真实活动订单重校验；`_handle_exit_authorize` 风险退出 `allowed_order_types=["limit"]`。
- 残留：HEDGE_READY 待总线 edb/ggr；`RISK_EXIT_MAX_SPEND` 占位(=0→风险退出受阻回退对冲)；C4 reconcile 身份不符 surfaced-不阻断(设计取舍)；C5 单轮多次取价未缓存。
- 验证：+`test_execution`(越价吃单/被动 maker/对冲 None 守门/对冲成交确认) ×4、+`test_run_cycle`(风险退出回退对冲/风险数据缺口/孤儿清理免门) ×3（另更新 2 例既有 monkeypatch 签名）；`run_all.py` → **210 passed, 0 failed**；`build_bundle.py --check` 通过。重生成覆盖交付物(v2.5.0) + 快照。

## R3 外部审计文档 Tier0（开真前安全 P0）— 完成（2026-06-22，STRATEGY_VERSION 2.6.0）
- 起因：外部文档《执行层开仓授权与分阶段自动化综合建议 v1.0》对 2.6.0(SHA a99cf6b) 审计。逐条核对后：架构方向(人工批准新增风险 + 自动降险 + 硬门)与现状一致、无需转向；其余 P0/P1 多为"开真前"且已部分做，**唯一真实漏修**为 3.2 裸短腿。本轮按用户选择只落 Tier0 两项。
- **3.2 / INV-03**：`_attempt_commit` 的 `ENTRY_ABANDONED` 原对任意 short_done 全退 prot_done → 部分短腿成交即留裸卖方。改为 short_done>0 时冻结 1:1 已覆盖较小持仓(short=prot=short_done)、只退多余保护 prot_done−short_done、`STRUCTURE_OPEN(PARTIAL_FROZEN)`；short_done≈0 才全退。任意时点 protection_qty≥short_qty。
- **3.3 / INV-04·INV-10**：`deribit_io.dbt_get_positions` 读失败返回 None(区别确实空[])；`startup_recovery_check` 持仓/活动订单读取 None→`_data_unknown_verdict`(DATA_UNKNOWN, allow_new_open=False)，不凭本地空判"交易所无仓"。best-effort 调用方(`_evaluate_hedge`/`ledger_reconcile`/manage opt_pos)仍 `or []` 不阻断降险。
- 范围：仅 Tier0；Tier1(统一真相/VRP+预算入库/有符号对冲/NewRiskApprovalPackage+TTL/捆绑降险授权/OFFLINE-live 守门) 与 Tier2(标定/动态再平衡/Class A-C/影子追踪/分阶段) 列为开真前路线，未一次性堆砌。
- 验证：+`test_run_cycle`(放弃部分成交冻结无裸短腿 / 持仓读失败→DATA_UNKNOWN / 确实空→OK) ×3（mock 增 `get_open_orders_by_currency`→[]）；`run_all.py` → **213 passed, 0 failed**；`build_bundle.py --check` 通过。重生成覆盖交付物(v2.6.0) + 快照 `spm_执行v2.6.0_外部审计Tier0.py`。

## R4 外部审计复核 → T0 扩展（恢复默认 + 信号方向权威）— 完成（2026-06-22，STRATEGY_VERSION 2.7.0）
- 起因：外部审计对 R3 两项 T0 复核——判定正确但应扩展：T0-B 恢复状态默认放行(fail-open)、T0-C 信号包方向未成为执行权威(FILE/G 下 side_hint 不驱动方向)。另指出里程碑应命名 `DRY_RUN_CANDIDATE` 而非可用。
- **T0-B**：`_recovery_verdict()` 默认由 `{OK, allow_new_open:True}` 改 `{RECOVERY_NOT_CHECKED, allow_new_open:False}`（恢复未检查即禁开新仓；持仓降险不受影响）。test `_setup` 增 `startup_recovery_check` 调用镜像生产启动（落 OK），既有建库测试在新默认下仍绿。
- **T0-C**：新增 `_signal_side_to_bias(side_hint)`；`_signal_allows_entry` 在 FILE/G 下要求 `side_hint` 映射方向 == `DIRECTION_BIAS` 否则 fail-closed（防 side_hint=put 却生成 call 价差）；OFFLINE_MANUAL 仍静态权威(仅 DRY_RUN)。完整"side_hint 驱动方向"(ExecutionSignalContext)随总线模式落地。
- 明确未做：T0-A 完整版(放弃时重读交易所+确认订单终态后冻结)couple 于 T0-LIVE(订单状态不可知禁重复下单)→真实资金前；T0-D(VRP 可注入/测试 fixture，禁生产硬写 vrp_pass=True)、`SyntheticFillAdapter` 合成成交→正式端到端空跑前；ACCOUNT_STATE_UNKNOWN 由"非 PM→无候选"间接 fail-closed。
- 里程碑修正：T0 后为 **DRY_RUN_CANDIDATE**（可单测/源码回归/静态方向盘口空跑；端到端空跑须补 T0-C 全版+T0-D；小额实盘须 Tier1）。
- 验证：+`test_run_cycle`(恢复未检查默认禁开 / FILE/G 方向不一致 fail-closed·一致放行) ×2；`run_all.py` → **215 passed, 0 failed**；`build_bundle.py --check` 通过。重生成覆盖交付物(v2.7.0) + 快照 `spm_执行v2.7.0_T0扩展.py`。
